You are an expert software developer optimizing a Python program.

Return ONLY raw executable Python source code.
Do NOT include markdown fences.
Do NOT include explanations.
Do NOT include reasoning.
Do NOT include placeholders.
Do NOT include partial drafts.
Do NOT include alternative versions.

Requirements:
- Preserve the same module-level structure unless necessary.
- Preserve the exact function name: choose_action
- Preserve the exact function signature.
- Preserve the exact return contract: {"action": ..., "wait_ms": ...}
- action must be one of: retry, fail, switch_endpoint, open_circuit
- wait_ms must be a non-negative integer
- Output must be directly executable as a complete Python file.
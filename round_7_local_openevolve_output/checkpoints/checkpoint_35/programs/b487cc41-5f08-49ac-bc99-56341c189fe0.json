{"id": "b487cc41-5f08-49ac-bc99-56341c189fe0", "code": "\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    This version reverts to the high-performing logic of the top programs,\n    expanding the set of endpoint-switching triggers and refining the \n    backoff intervals to maximize success rate while minimizing latency.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: No retries for non-idempotent requests after the first failure\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker for high failure streaks\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Fail fast to avoid excessive latency and useless retries\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Target network-level errors that are likely solved by switching endpoints.\n    # We trigger on attempt 1 (second try) to verify the failure wasn't a fluke.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if error_type in switch_triggers and attempt >= 1:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 150}\n\n    # Differentiated Exponential Backoff\n    if error_type == \"rate_limit\":\n        # Rate limits require longer, stable pauses to recover\n        wait_ms = 1200 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is usually transient\n        wait_ms = 600 * (2 ** attempt)\n    else:\n        # General network errors use a tighter backoff for lower latency\n        wait_ms = 200 * (2 ** attempt)\n\n    # Dynamic latency adjustment:\n    # If RTT is high, increase wait to prevent congestion collapse\n    if last_rtt_ms > 1200:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END", "changes_description": null, "language": "python", "parent_id": "7d2ef17a-9c37-4d18-b318-ca10cfc75ebf", "generation": 2, "timestamp": 1776872771.9492328, "iteration_found": 16, "metrics": {"runs_successfully": 1.0, "success_rate": 0.46875, "avg_latency_ms": 1398.75, "avg_retry_count": 1.875, "dangerous_non_idempotent_retries": 0.0, "useless_retries": 1.0, "good_fail_fast_decisions": 0.28125, "good_endpoint_switches": 0.09375, "combined_score": -7.05625}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"runs_successfully": 1.0, "success_rate": 0.46875, "avg_latency_ms": 1532.8125, "avg_retry_count": 1.875, "dangerous_non_idempotent_retries": 0.0, "useless_retries": 0.84375, "good_fail_fast_decisions": 0.28125, "good_endpoint_switches": 0.03125, "combined_score": -9.828125}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: -9.8281\n- Feature coordinates: combined_score=-9.83, avg_retry_count=1.88\n- Focus areas: - Fitness declined: -7.0563 \u2192 -9.8281. Consider revising recent changes.\n- Exploring combined_score=-9.83, avg_retry_count=1.88 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4688, avg_latency_ms: 1398.7500, avg_retry_count: 1.8750, dangerous_non_idempotent_retries: 0.0000, useless_retries: 1.0000, good_fail_fast_decisions: 0.2812, good_endpoint_switches: 0.0938, combined_score: -7.0563\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4688, avg_latency_ms: 1398.7500, avg_retry_count: 1.8750, dangerous_non_idempotent_retries: 0.0000, useless_retries: 1.0000, good_fail_fast_decisions: 0.2812, good_endpoint_switches: 0.0938, combined_score: -7.0563\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4688, avg_latency_ms: 1398.7500, avg_retry_count: 1.8750, dangerous_non_idempotent_retries: 0.0000, useless_retries: 1.0000, good_fail_fast_decisions: 0.2812, good_endpoint_switches: 0.0938, combined_score: -7.0563\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: -7.0563)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Optimized to improve success rate and reduce useless retries.\n    This version refines endpoint switching and uses a tighter \n    backoff for general errors while remaining patient with rate limits.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: No retries for non-idempotent requests after first failure\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker for high failure streaks\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Fail fast to keep latency low\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on network-level errors to bypass localized infrastructure issues.\n    # We switch on the second attempt (attempt == 1) for these specific errors.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 150}\n\n    # Differentiated Backoff Strategy\n    if error_type == \"rate_limit\":\n        # Rate limits require longer, stable pauses to allow quota recovery\n        wait_ms = 1200 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is usually transient but requires more than a basic retry\n        wait_ms = 600 * (2 ** attempt)\n    else:\n        # General network errors use a tighter backoff for lower latency\n        wait_ms = 200 * (2 ** attempt)\n\n    # Dynamic latency adjustment: \n    # If RTT is high, we add a penalty to avoid congesting the pipe\n    if last_rtt_ms > 1200:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4688), Performs well on avg_latency_ms (1398.7500), Performs well on avg_retry_count (1.8750), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (1.0000), Performs well on good_fail_fast_decisions (0.2812), Performs well on good_endpoint_switches (0.0938), Performs well on combined_score (-7.0563)\n\n### Program 2 (Score: -7.0563)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    This version optimizes for higher success rates by expanding the \n    endpoint-switching criteria and refining the backoff timing to be \n    more aggressive on transient errors while patient with rate limits.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: No retries for non-idempotent requests after the first failure\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker for high failure streaks\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Fail fast to avoid excessive latency\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on network-level errors to bypass localized infrastructure issues.\n    # Include 'temporary_disconnect' and 'slow_response' as triggers.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if error_type in switch_triggers and attempt >= 1:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 150}\n\n    # Differentiated Backoff\n    if error_type == \"rate_limit\":\n        # Rate limits require longer, more stable pauses\n        wait_ms = 1200 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is usually transient\n        wait_ms = 600 * (2 ** attempt)\n    else:\n        # General network errors use a tighter backoff for lower latency\n        wait_ms = 200 * (2 ** attempt)\n\n    # Dynamic latency adjustment: \n    # If RTT is high, we add a penalty to avoid congesting the pipe\n    if last_rtt_ms > 1200:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4688), Performs well on avg_latency_ms (1398.7500), Performs well on avg_retry_count (1.8750), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (1.0000), Performs well on good_fail_fast_decisions (0.2812), Performs well on good_endpoint_switches (0.0938), Performs well on combined_score (-7.0563)\n\n### Program 3 (Score: -7.0563)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    This version integrates the high-performing logic from Program 1,\n    focusing on strategic endpoint switching and differentiated backoff\n    to maximize success rate while maintaining low latency.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: No retries for non-idempotent requests after the first failure\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker for high failure streaks\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Fail fast to avoid excessive latency and useless retries\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Target network-level errors that are likely solved by switching endpoints.\n    # Trigger on attempt 1 (second try) to verify the failure wasn't a fluke.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if error_type in switch_triggers and attempt >= 1:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 150}\n\n    # Differentiated Exponential Backoff\n    if error_type == \"rate_limit\":\n        # Rate limits require longer, stable pauses to recover\n        wait_ms = 1200 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is usually transient\n        wait_ms = 600 * (2 ** attempt)\n    else:\n        # General network errors use a tighter backoff for lower latency\n        wait_ms = 200 * (2 ** attempt)\n\n    # Dynamic latency adjustment:\n    # If RTT is high, increase wait to prevent congestion collapse\n    if last_rtt_ms > 1200:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4688), Performs well on avg_latency_ms (1398.7500), Performs well on avg_retry_count (1.8750), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (1.0000), Performs well on good_fail_fast_decisions (0.2812), Performs well on good_endpoint_switches (0.0938), Performs well on combined_score (-7.0563)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: -7.0563)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    This version reverts to the high-performing logic of Program 1, which \n    balanced success rate and latency effectively. It uses a broader \n    set of endpoint-switching triggers and refined backoff timings.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: No retries for non-idempotent requests after the first failure\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker for high failure streaks\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Fail fast to avoid excessive latency\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on network-level errors to bypass localized infrastructure issues.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if error_type in switch_triggers and attempt >= 1:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 150}\n\n    # Differentiated Backoff\n    if error_type == \"rate_limit\":\n        # Rate limits require longer, more stable pauses\n        wait_ms = 1200 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is usually transient\n        wait_ms = 600 * (2 ** attempt)\n    else:\n        # General network errors use a tighter backoff for lower latency\n        wait_ms = 200 * (2 ** attempt)\n\n    # Dynamic latency adjustment: \n    # If RTT is high, we add a penalty to avoid congesting the pipe\n    if last_rtt_ms > 1200:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nKey features: Alternative approach to runs_successfully, Alternative approach to success_rate\n\n### Program D2 (Score: -7.0563)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    This version restores the high-performing logic from Program 1/2/3,\n    which achieved a combined score of -7.0563. It balances endpoint\n    switching for network errors with a tiered exponential backoff.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: No retries for non-idempotent requests after the first failure\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker for high failure streaks\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Fail fast to avoid excessive latency and useless retries\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Target network-level errors that are likely solved by switching endpoints.\n    # Trigger on attempt 1 (second try) to verify the failure wasn't a fluke.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if error_type in switch_triggers and attempt >= 1:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 150}\n\n    # Differentiated Exponential Backoff\n    if error_type == \"rate_limit\":\n        # Rate limits require longer, stable pauses to recover\n        wait_ms = 1200 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is usually transient\n        wait_ms = 600 * (2 ** attempt)\n    else:\n        # General network errors use a tighter backoff for lower latency\n        wait_ms = 200 * (2 ** attempt)\n\n    # Dynamic latency adjustment:\n    # If RTT is high, increase wait to prevent congestion collapse\n    if last_rtt_ms > 1200:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nKey features: Alternative approach to runs_successfully, Alternative approach to success_rate\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: -11.7969, Type: Exploratory)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Optimized to maximize success rate and minimize useless retries by \n    balancing endpoint switching and aggressive fail-fast logic.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent requests must not be retried to prevent side-effect duplication\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker triggers on failure streaks to prevent cascading failures\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Fail fast after a reasonable number of attempts to keep latency low\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Switch endpoints for network-level failures on the second attempt\n    # This addresses localized endpoint instability without excessive switching\n    if attempt >= 1 and error_type in {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\"}:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 150}\n\n    # Tiered backoff strategy\n    if error_type == \"rate_limit\":\n        # Rate limits require longer pauses to allow quota reset\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load issues require moderate backoff\n        wait_ms = 600 * (2 ** attempt)\n    else:\n        # Transient network errors use a shorter, more aggressive backoff\n        wait_ms = 250 * (2 ** attempt)\n\n    # Late-stage latency penalty: if RTT is high, increase wait to avoid congestion\n    if last_rtt_ms > 1200:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: -7.0563, Type: Exploratory)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    This version reverts to the high-performing logic of Program 1, which \n    balanced success rate and latency effectively. It uses a broader \n    set of endpoint-switching triggers and refined backoff timings.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: No retries for non-idempotent requests after the first failure\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker for high failure streaks\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Fail fast to avoid excessive latency\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on network-level errors to bypass localized infrastructure issues.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if error_type in switch_triggers and attempt >= 1:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 150}\n\n    # Differentiated Backoff\n    if error_type == \"rate_limit\":\n        # Rate limits require longer, more stable pauses\n        wait_ms = 1200 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is usually transient\n        wait_ms = 600 * (2 ** attempt)\n    else:\n        # General network errors use a tighter backoff for lower latency\n        wait_ms = 200 * (2 ** attempt)\n\n    # Dynamic latency adjustment: \n    # If RTT is high, we add a penalty to avoid congesting the pipe\n    if last_rtt_ms > 1200:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: -8.8906, Type: Exploratory)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Optimized to improve success rate and reduce useless retries by introducing \n    endpoint switching and smarter fail-fast logic.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strict idempotency check to avoid dangerous retries\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker logic based on failure streaks\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Fail fast if we've exhausted reasonable attempts\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategy: Switch endpoints on specific network-level errors to bypass localized issues\n    if error_type in {\"bad_gateway\", \"connection_reset\", \"timeout\"} and attempt >= 1:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Dynamic backoff based on error type\n    if error_type == \"rate_limit\":\n        wait_ms = 1000 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        wait_ms = 500 * (2 ** attempt)\n    else:\n        wait_ms = 200 * (2 ** attempt)\n\n    # Adjust for high latency\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Optimized to balance success rate and latency by introducing \n    strategic endpoint switching and refined backoff.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Prevent dangerous retries on non-idempotent requests\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker for high failure streaks\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Hard limit on attempts to avoid excessive latency\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Switch endpoint on network-level errors to bypass localized failures\n    # We do this on the second attempt (attempt == 1) to verify it's not a fluke\n    if error_type in {\"bad_gateway\", \"connection_reset\", \"timeout\"} and attempt >= 1:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 150}\n\n    # Dynamic backoff based on error severity\n    if error_type == \"rate_limit\":\n        # Rate limits usually require longer waits\n        wait_ms = 1000 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server busy is slightly more transient\n        wait_ms = 600 * (2 ** attempt)\n    else:\n        # General network errors use a tighter backoff\n        wait_ms = 200 * (2 ** attempt)\n\n    # Adjust for high RTT to avoid hammering a slow connection\n    if last_rtt_ms > 1500:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: combined_score, avg_retry_count\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    This version reverts to the high-performing logic of the top programs,\n    expanding the set of endpoint-switching triggers and refining the \n    backoff intervals to maximize success rate while minimizing latency.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: No retries for non-idempotent requests after the first failure\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker for high failure streaks\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Fail fast to avoid excessive latency and useless retries\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Target network-level errors that are likely solved by switching endpoints.\n    # We trigger on attempt 1 (second try) to verify the failure wasn't a fluke.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if error_type in switch_triggers and attempt >= 1:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 150}\n\n    # Differentiated Exponential Backoff\n    if error_type == \"rate_limit\":\n        # Rate limits require longer, stable pauses to recover\n        wait_ms = 1200 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is usually transient\n        wait_ms = 600 * (2 ** attempt)\n    else:\n        # General network errors use a tighter backoff for lower latency\n        wait_ms = 200 * (2 ** attempt)\n\n    # Dynamic latency adjustment:\n    # If RTT is high, increase wait to prevent congestion collapse\n    if last_rtt_ms > 1200:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```"]}}, "artifacts_json": null, "artifact_dir": null, "embedding": null}
{"id": "bf065f0c-1e27-4f6d-8fee-808a1a2e84c4", "code": "\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"\n    Optimized retry policy based on high-performing historical data.\n    \n    Key Strategies:\n    1. Aggressive Fail-Fast: Capping total attempts at 2 (attempt 0 and 1) \n       to drastically reduce 'useless_retries' and 'avg_latency_ms'.\n    2. Precision Switching: Targeting infrastructure errors on the first \n       retry to maximize 'good_endpoint_switches'.\n    3. Idempotency Safety: Zero tolerance for retrying non-idempotent requests.\n    4. Circuit Breaking: Rapidly isolating failing systems on a streak of 3.\n    \"\"\"\n    # Immediate exit for circuit breakers or non-retryable errors\n    if circuit_open or error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Never retry non-idempotent requests (only the initial attempt is allowed)\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: Trigger rapidly on failure streaks to prevent cascading\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # Fail fast: This is the critical driver for high combined_score.\n    # Limit to 2 attempts (attempt 0, 1). If attempt is 2, we fail immediately.\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on the first retry for network-level failures to bypass bad nodes.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered Backoff for the first retry (attempt 0 -> 1)\n    if error_type == \"rate_limit\":\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance for high RTT\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END", "changes_description": null, "language": "python", "parent_id": "3aa3c9dc-9661-4707-afa2-bea2a669e760", "generation": 3, "timestamp": 1776873548.486335, "iteration_found": 34, "metrics": {"runs_successfully": 1.0, "success_rate": 0.46875, "avg_latency_ms": 1136.5625, "avg_retry_count": 1.5, "dangerous_non_idempotent_retries": 0.0, "useless_retries": 0.65625, "good_fail_fast_decisions": 0.40625, "good_endpoint_switches": 0.09375, "combined_score": 6.184375}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"runs_successfully": 1.0, "success_rate": 0.46875, "avg_latency_ms": 1772.8125, "avg_retry_count": 1.875, "dangerous_non_idempotent_retries": 0.0, "useless_retries": 1.0, "good_fail_fast_decisions": 0.3125, "good_endpoint_switches": 0.09375, "combined_score": -18.028125}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: -18.0281\n- Feature coordinates: combined_score=-18.03, avg_retry_count=1.88\n- Focus areas: - Fitness declined: 6.1844 \u2192 -18.0281. Consider revising recent changes.\n- Exploring combined_score=-18.03, avg_retry_count=1.88 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4688, avg_latency_ms: 1136.5625, avg_retry_count: 1.5000, dangerous_non_idempotent_retries: 0.0000, useless_retries: 0.6562, good_fail_fast_decisions: 0.4062, good_endpoint_switches: 0.0938, combined_score: 6.1844\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4688, avg_latency_ms: 1136.5625, avg_retry_count: 1.5000, dangerous_non_idempotent_retries: 0.0000, useless_retries: 0.6562, good_fail_fast_decisions: 0.4062, good_endpoint_switches: 0.0938, combined_score: 6.1844\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4688, avg_latency_ms: 1136.5625, avg_retry_count: 1.5000, dangerous_non_idempotent_retries: 0.0000, useless_retries: 0.6562, good_fail_fast_decisions: 0.4062, good_endpoint_switches: 0.0938, combined_score: 6.1844\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 6.1844)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"\n    Optimized retry policy to maximize combined_score.\n    \n    Improvements:\n    1. Aggressive Fail-Fast: Strictly limit to 2 attempts (0, 1) to minimize useless_retries.\n    2. High-Precision Switching: Target infra errors immediately on attempt 1.\n    3. Idempotency Guard: zero tolerance for non-idempotent retries.\n    4. Dynamic Backoff: Adjusted wait times for specific error types.\n    \"\"\"\n    # Immediate exit for circuit breakers or non-retryable errors\n    if circuit_open or error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Never retry non-idempotent requests (only attempt 0 is allowed)\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: Trigger rapidly on failure streaks\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # Fail fast: This is the critical driver for reducing 'useless_retries' and latency\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on the first retry for network-level failures to bypass bad nodes.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered Backoff\n    if error_type == \"rate_limit\":\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance for high RTT\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4688), Performs well on avg_latency_ms (1136.5625), Performs well on avg_retry_count (1.5000), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (0.6562), Performs well on good_fail_fast_decisions (0.4062), Performs well on good_endpoint_switches (0.0938), Performs well on combined_score (6.1844)\n\n### Program 2 (Score: 6.1844)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"\n    Optimized retry policy focused on maximizing combined_score.\n    \n    Strategy:\n    1. Strict Fail-Fast: Limit total attempts to 2 (attempt 0 and 1) to minimize useless_retries.\n    2. High-Precision Switching: Target infra-errors on the first retry to bypass faulty nodes.\n    3. Idempotency Safety: Zero tolerance for non-idempotent retries.\n    4. Circuit Breaking: Rapidly isolate failing systems.\n    \"\"\"\n    # Immediate fail conditions\n    if circuit_open or error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Never retry non-idempotent requests\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: Trigger on 3 consecutive failures\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # Fail fast: Cap at 2 attempts to optimize latency and useless_retries metric\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on the first retry (attempt 1) for network/gateway errors.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered Backoff\n    if error_type == \"rate_limit\":\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance for high RTT\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4688), Performs well on avg_latency_ms (1136.5625), Performs well on avg_retry_count (1.5000), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (0.6562), Performs well on good_fail_fast_decisions (0.4062), Performs well on good_endpoint_switches (0.0938), Performs well on combined_score (6.1844)\n\n### Program 3 (Score: 6.1844)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Reverting to the high-performing strategy from Program 1/2/3.\n    The core of the high fitness score was a strict attempt limit (attempt < 2)\n    and precise endpoint switching for network-level errors.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Never retry non-idempotent requests beyond the first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: Trigger on 3 consecutive failures to prevent cascading\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # Fail fast: Limit to 2 attempts total (attempt 0, 1). \n    # This is the critical change to reduce 'useless_retries' and lower latency.\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on the first retry (attempt 1) for infrastructure-level errors.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered Exponential Backoff\n    if error_type == \"rate_limit\":\n        # Rate limits need substantial time to reset\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is transient but needs a small gap\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        # Fast recovery for transient network glitches\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance: Add delay if the network is showing high latency\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4688), Performs well on avg_latency_ms (1136.5625), Performs well on avg_retry_count (1.5000), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (0.6562), Performs well on good_fail_fast_decisions (0.4062), Performs well on good_endpoint_switches (0.0938), Performs well on combined_score (6.1844)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 6.1844)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Optimized to maximize combined_score by minimizing useless_retries and \n    latency while maintaining a high success rate.\n    \"\"\"\n    # Immediate fail if circuit is open or error is non-retryable\n    if circuit_open or error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Strictly forbid retries for non-idempotent requests after attempt 0\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: Trigger on 3 consecutive failures to avoid hammering a dead system\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # Fail fast: Limit to 2 attempts total (attempt 0, 1).\n    # This is the key to reducing 'useless_retries' and 'avg_latency_ms'.\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on the first retry (attempt 1) for infrastructure-level errors.\n    # This is highly effective for 'bad_gateway' or 'connection_reset'.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered Backoff based on error severity\n    if error_type == \"rate_limit\":\n        # Rate limits require a significant pause to allow the window to reset\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server overload is usually transient but needs a small gap\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        # Fast recovery for transient network glitches\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance: Add small delay if the network is showing high latency\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nKey features: Alternative approach to runs_successfully, Alternative approach to success_rate\n\n### Program D2 (Score: 6.1844)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Refined to maximize fitness by aggressively pruning useless retries\n    and optimizing endpoint switching for network-level failures.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Never retry non-idempotent requests\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: Trigger on a moderate streak to prevent cascading failure\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # Fail fast: Limit to 2 attempts total to minimize 'useless_retries' and latency\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on the first retry (attempt 1) for infrastructure-level errors.\n    # This bypasses bad nodes quickly.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered Exponential Backoff\n    if error_type == \"rate_limit\":\n        # Rate limits need substantial time to reset\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is transient but needs a small gap\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        # Fast recovery for transient network glitches\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance: Add delay if the network is showing high latency\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nKey features: Alternative approach to runs_successfully, Alternative approach to success_rate\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 6.1844, Type: High-Performer)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"\n    Optimized retry policy.\n    \n    Strategy:\n    - Minimize 'useless_retries' by capping attempts at 2.\n    - Maximize 'good_endpoint_switches' by targeting infra errors on attempt 1.\n    - Ensure 0 'dangerous_non_idempotent_retries'.\n    - Use targeted backoff to balance success rate and latency.\n    \"\"\"\n    # 1. Immediate Failures\n    if circuit_open or error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # 2. Idempotency Safety (Critical for fitness)\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # 3. Circuit Breaker\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # 4. Fail-Fast Limit\n    # Capping at attempt 2 (meaning 0 and 1 are tried) reduces useless_retries significantly.\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # 5. Endpoint Switching\n    # Target network-level failures on the first retry to bypass faulty nodes.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # 6. Backoff Logic\n    if error_type == \"rate_limit\":\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: -15.3594, Type: Exploratory)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"\n    Optimized retry policy to maximize fitness by balancing success rate, \n    latency, and fail-fast accuracy.\n    \"\"\"\n    # Circuit breaker: immediate fail if open\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors: immediate fail\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: No retries for non-idempotent requests after the first failure\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker trigger: high consecutive failure count\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5000}\n\n    # Hard limit on attempts to prevent excessive latency (fail-fast)\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on network/gateway errors to bypass localized infrastructure issues.\n    # Trigger on attempt 1 to avoid premature switching on a single fluke.\n    if error_type in {\"bad_gateway\", \"connection_reset\", \"timeout\"} and attempt >= 1:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Differentiated Exponential Backoff\n    if error_type == \"rate_limit\":\n        # Rate limits require more patience\n        wait_ms = 1200 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is usually transient but needs breathing room\n        wait_ms = 600 * (2 ** attempt)\n    else:\n        # General network issues (timeouts, disconnects) use tighter backoff\n        wait_ms = 250 * (2 ** attempt)\n\n    # Latency Adjustment:\n    # If the network is slow (high RTT), increase wait time to avoid congestion.\n    if last_rtt_ms > 1200:\n        wait_ms += 300\n\n    # Cap the wait time to 10s to keep latency within reasonable bounds\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10000)}\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: -11.7969, Type: Exploratory)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Optimized to maximize success rate and minimize useless retries by \n    balancing endpoint switching and aggressive fail-fast logic.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent requests must not be retried to prevent side-effect duplication\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker triggers on failure streaks to prevent cascading failures\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Fail fast after a reasonable number of attempts to keep latency low\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Switch endpoints for network-level failures on the second attempt\n    # This addresses localized endpoint instability without excessive switching\n    if attempt >= 1 and error_type in {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\"}:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 150}\n\n    # Tiered backoff strategy\n    if error_type == \"rate_limit\":\n        # Rate limits require longer pauses to allow quota reset\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load issues require moderate backoff\n        wait_ms = 600 * (2 ** attempt)\n    else:\n        # Transient network errors use a shorter, more aggressive backoff\n        wait_ms = 250 * (2 ** attempt)\n\n    # Late-stage latency penalty: if RTT is high, increase wait to avoid congestion\n    if last_rtt_ms > 1200:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Optimized to maximize success rate while minimizing useless retries and latency.\n    Focuses on aggressive fail-fast for non-idempotent requests and strategic \n    endpoint switching for network-level transient errors.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety first: Never retry non-idempotent requests\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker triggers on persistent failure streaks\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3_000}\n\n    # Fail fast after 3 attempts to prevent latency bloat\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching: \n    # Switch on the first retry for network-level errors to bypass bad nodes quickly.\n    if attempt >= 1 and error_type in {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\"}:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered backoff based on error profile\n    if error_type == \"rate_limit\":\n        # Rate limits need significant cooling off\n        wait_ms = 1200 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Busy servers need a moderate pause\n        wait_ms = 600 * (2 ** attempt)\n    else:\n        # General network issues use a tighter loop\n        wait_ms = 200 * (2 ** attempt)\n\n    # Latency-aware adjustment: avoid hammering a congested path\n    if last_rtt_ms > 1200:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 8_000)}\n# EVOLVE-BLOCK-END\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: combined_score, avg_retry_count\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"\n    Optimized retry policy based on high-performing historical data.\n    \n    Key Strategies:\n    1. Aggressive Fail-Fast: Capping total attempts at 2 (attempt 0 and 1) \n       to drastically reduce 'useless_retries' and 'avg_latency_ms'.\n    2. Precision Switching: Targeting infrastructure errors on the first \n       retry to maximize 'good_endpoint_switches'.\n    3. Idempotency Safety: Zero tolerance for retrying non-idempotent requests.\n    4. Circuit Breaking: Rapidly isolating failing systems on a streak of 3.\n    \"\"\"\n    # Immediate exit for circuit breakers or non-retryable errors\n    if circuit_open or error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Never retry non-idempotent requests (only the initial attempt is allowed)\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: Trigger rapidly on failure streaks to prevent cascading\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # Fail fast: This is the critical driver for high combined_score.\n    # Limit to 2 attempts (attempt 0, 1). If attempt is 2, we fail immediately.\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on the first retry for network-level failures to bypass bad nodes.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered Backoff for the first retry (attempt 0 -> 1)\n    if error_type == \"rate_limit\":\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance for high RTT\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```"]}}, "artifacts_json": null, "artifact_dir": null, "embedding": null}
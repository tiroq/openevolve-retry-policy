{"id": "84fb7113-b24b-4fee-acee-2feac84d1d35", "code": "\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall time limit check - prevent runaway operations\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Total attempts limit - prevent infinite loops\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        # More aggressive rate limiting - shorter waits\n        wait_ms = min(400 * (2 ** attempt), 6_000)\n    elif error_type == \"server_busy\":\n        # Moderate wait for server_busy errors\n        wait_ms = min(200 * (2 ** attempt), 3_500)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        # Slightly shorter waits for network-related errors\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n    else:\n        # Default for other error types\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n\n    # RTT-based adjustment - only add delay for very high latency\n    if last_rtt_ms > 2_500:\n        wait_ms = min(wait_ms + 150, 6_150)\n    elif last_rtt_ms > 1_500:\n        # Small adjustment for moderate latency\n        wait_ms = min(wait_ms + 50, 1_650)\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END", "changes_description": null, "language": "python", "parent_id": "e61098fc-45ff-4acf-af03-07daa55f0d18", "generation": 3, "timestamp": 1776876696.726402, "iteration_found": 22, "metrics": {"runs_successfully": 1.0, "success_rate": 0.40625, "avg_latency_ms": 1171.875, "avg_retry_count": 2.0625, "dangerous_non_idempotent_retries": 0.0, "useless_retries": 0.6875, "good_fail_fast_decisions": 0.28125, "good_endpoint_switches": 0.0, "combined_score": -4.3125}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"runs_successfully": 1.0, "success_rate": 0.40625, "avg_latency_ms": 1320.0, "avg_retry_count": 2.03125, "dangerous_non_idempotent_retries": 0.0, "useless_retries": 0.6875, "good_fail_fast_decisions": 0.28125, "good_endpoint_switches": 0.0, "combined_score": -8.678125}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: -8.6781\n- Feature coordinates: combined_score=-8.68, avg_retry_count=2.03\n- Focus areas: - Fitness unchanged at -8.6781\n- Exploring combined_score=-8.68, avg_retry_count=2.03 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4062, avg_latency_ms: 1320.0000, avg_retry_count: 2.0312, dangerous_non_idempotent_retries: 0.0000, useless_retries: 0.6875, good_fail_fast_decisions: 0.2812, good_endpoint_switches: 0.0000, combined_score: -8.6781\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4062, avg_latency_ms: 1320.0000, avg_retry_count: 2.0312, dangerous_non_idempotent_retries: 0.0000, useless_retries: 0.6875, good_fail_fast_decisions: 0.2812, good_endpoint_switches: 0.0000, combined_score: -8.6781\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Unknown changes\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4062, avg_latency_ms: 1320.0000, avg_retry_count: 2.0312, dangerous_non_idempotent_retries: 0.0000, useless_retries: 0.6875, good_fail_fast_decisions: 0.2812, good_endpoint_switches: 0.0000, combined_score: -8.6781\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: -8.6781)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type == \"rate_limit\":\n        wait_ms = min(500 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(250 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n\n    if last_rtt_ms > 2_000:\n        wait_ms += 250\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4062), Performs well on avg_latency_ms (1320.0000), Performs well on avg_retry_count (2.0312), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (0.6875), Performs well on good_fail_fast_decisions (0.2812), Performs well on good_endpoint_switches (0.0000), Performs well on combined_score (-8.6781)\n\n### Program 2 (Score: -8.6781)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall time limit check for latency improvement\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Total attempts limit - prevent infinite loops\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(500 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(250 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n\n    # RTT-based adjustment for high latency\n    if last_rtt_ms > 2_000:\n        wait_ms = min(wait_ms + 250, 8_500)\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4062), Performs well on avg_latency_ms (1320.0000), Performs well on avg_retry_count (2.0312), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (0.6875), Performs well on good_fail_fast_decisions (0.2812), Performs well on good_endpoint_switches (0.0000), Performs well on combined_score (-8.6781)\n\n### Program 3 (Score: -8.6781)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Hard limits to prevent runaway retry behavior\n    # Total attempts limit\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n    \n    # Consecutive failures limit - more aggressive than baseline\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(500 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(250 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n\n    # Small RTT adjustment only for high latency\n    if last_rtt_ms > 2_000:\n        wait_ms = min(wait_ms + 250, 8_500)\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4062), Performs well on avg_latency_ms (1320.0000), Performs well on avg_retry_count (2.0312), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (0.6875), Performs well on good_fail_fast_decisions (0.2812), Performs well on good_endpoint_switches (0.0000), Performs well on combined_score (-8.6781)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: -8.6781)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Limit total attempts\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(500 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(250 * (2 ** attempt), 4_000)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n    else:\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n\n    # Small RTT adjustment only for high latency\n    if last_rtt_ms > 2_000:\n        wait_ms = min(wait_ms + 250, 8_500)\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n```\nKey features: Alternative approach to runs_successfully, Alternative approach to success_rate\n\n### Program D2 (Score: -8.6781)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Hard limits to prevent runaway retry behavior\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(500 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(250 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n\n    # Small RTT adjustment only for high latency\n    if last_rtt_ms > 2_000:\n        wait_ms = min(wait_ms + 250, 8_500)\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n```\nKey features: Alternative approach to runs_successfully, Alternative approach to success_rate\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: -1000000000.0000, Type: Exploratory)\n```python\nLooking at the current program and the evolution history, I can see that:\n\n1. Current score: -8.6781 (Program 1)\n2. Previous attempts achieved -12.6156 (Program 3) and -42.4375 (Program D2)\n3. The system wants improvement while maintaining diversity in retry_count\n\nThe key differences between the current program and better-performing ones are:\n- Better programs check `elapsed_ms` for overall time limits\n- Better programs open circuit at `consecutive_failures >= 4` instead of 5\n- Better programs use different base wait multipliers and caps\n- Better programs add RTT-based endpoint switching logic\n- Better programs handle different error types more specifically\n\nHowever, the task is to improve the current program's fitness, so I'll incorporate improvements from the better-performing programs while maintaining the structure that achieved -8.6781.\n\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if we've been trying too long overall\n    if elapsed_ms > 12_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 4_000}\n\n    # Limit total attempts based on idempotency\n    max_attempts = 4 if is_idempotent else 3\n    if attempt >= max_attempts:\n        # For idempotent operations, try switching endpoint instead of failing\n        if is_idempotent and attempt < 4:\n            return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(600 * (2 ** attempt), 9_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(300 * (2 ** attempt), 5_000)\n    elif error_type == \"timeout\":\n        wait_ms = min(150 * (2 ** attempt), 3_000)\n    elif error_type == \"connection_reset\":\n        wait_ms = min(120 * (2 ** attempt), 2_500)\n    else:\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n\n    # RTT-based adjustments - more aggressive than original\n    if last_rtt_ms > 2_500:\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: -8.6781, Type: Exploratory)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall time limit check for latency improvement\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Total attempts limit - prevent infinite loops\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(500 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(250 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n\n    # RTT-based adjustment for high latency\n    if last_rtt_ms > 2_000:\n        wait_ms = min(wait_ms + 250, 8_500)\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: -1000000000.0000, Type: Exploratory)\n```python\nLooking at the current program and the evolution history, I can see that the fitness score declined from -8.6781 to -12.6156. The key issues appear to be:\n\n1. The current program has more complex logic that may be causing suboptimal decisions\n2. The elapsed_ms threshold (15,000) is too high compared to better-performing versions\n3. The circuit opening threshold (consecutive_failures >= 4) with wait_ms=3_000 may not be optimal\n4. The RTT-based endpoint switching logic may be too aggressive\n\nBased on the top-performing programs (all scoring -8.6781), I'll create a cleaned-up version that:\n- Uses a more conservative elapsed_ms threshold (10,000 as in Program 2)\n- Opens circuit at consecutive_failures >= 5 (as in Program 1)\n- Simplifies the backoff logic while keeping error-type specificity\n- Removes the complex RTT-based endpoint switching logic that seems to hurt performance\n- Maintains the good structure from Program 2 but with slight optimizations\n\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall time limit - prevent runaway operations\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Total attempts limit - prevent infinite loops\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(500 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(250 * (2 ** attempt), 4_000)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n    else:\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n\n    # RTT-based adjustment for high latency\n    if last_rtt_ms > 2_000:\n        wait_ms = min(wait_ms + 250, 8_500)\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Hard limits to prevent runaway retry behavior\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(500 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(250 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n\n    # Small RTT adjustment only for high latency\n    if last_rtt_ms > 2_000:\n        wait_ms = min(wait_ms + 250, 8_500)\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: combined_score, avg_retry_count\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["Looking at the current program and the evolution history, I can see that all top-performing programs have a score of -8.6781 with very similar metrics. The key differences between programs that maintain this score are subtle variations in the retry logic and thresholds.\n\nTo improve fitness while maintaining diversity, I'll focus on optimizing the `avg_retry_count` dimension while keeping the same `combined_score`. The current program has `avg_retry_count=2.03`, so I'll try a slightly more aggressive retry strategy that might reduce retries while maintaining success rate.\n\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall time limit check - prevent runaway operations\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Total attempts limit - prevent infinite loops\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        # More aggressive rate limiting - shorter waits\n        wait_ms = min(400 * (2 ** attempt), 6_000)\n    elif error_type == \"server_busy\":\n        # Moderate wait for server_busy errors\n        wait_ms = min(200 * (2 ** attempt), 3_500)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        # Slightly shorter waits for network-related errors\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n    else:\n        # Default for other error types\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n\n    # RTT-based adjustment - only add delay for very high latency\n    if last_rtt_ms > 2_500:\n        wait_ms = min(wait_ms + 150, 6_150)\n    elif last_rtt_ms > 1_500:\n        # Small adjustment for moderate latency\n        wait_ms = min(wait_ms + 50, 1_650)\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n```"]}}, "artifacts_json": null, "artifact_dir": null, "embedding": null}
{"id": "b6441302-2141-41ae-aaad-bf28226d90ff", "code": "Looking at the current situation, I can see the program has crashed to -1,000,000,000.0000 fitness with avg_retry_count=999.00, indicating an infinite loop or unbounded retry behavior. This is likely caused by an incomplete RTT-based adjustment logic that leaves the function without a proper return statement in some code paths.\n\nThe top performers consistently achieve -4.3125 with avg_retry_count=2.0625 by:\n1. Maintaining strict time limits (10,000ms)\n2. Using circuit breaker at consecutive_failures >= 5\n3. Properly handling all error types with specific strategies\n4. Ensuring all code paths return valid actions\n5. Keeping RTT adjustments conservative\n\nLet me create a robust solution that fixes the infinite loop issue while maintaining the successful patterns:\n\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Safety checks first - prevent infinite loops and runaway behavior\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall time limit - prevent runaway operations\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Total attempts limit - prevent infinite loops\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(400 * (2 ** attempt), 6_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(200 * (2 ** attempt), 3_500)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n    elif error_type == \"bad_gateway\":\n        wait_ms = min(150 * (2 ** attempt), 2_500)\n    elif error_type == \"temporary_disconnect\":\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n    elif error_type == \"slow_response\":\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n    else:\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n\n    # RTT-based adjustment - only add delay for very high latency\n    if last_rtt_ms > 2_50", "changes_description": null, "language": "python", "parent_id": "16688e8e-db59-4bb8-b740-026aba3a2beb", "generation": 3, "timestamp": 1776877491.819606, "iteration_found": 40, "metrics": {"runs_successfully": 0.0, "success_rate": 0.0, "avg_latency_ms": 999999.0, "avg_retry_count": 999.0, "dangerous_non_idempotent_retries": 999.0, "useless_retries": 999.0, "good_fail_fast_decisions": 0.0, "good_endpoint_switches": 0.0, "combined_score": -1000000000.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"runs_successfully": 0.0, "success_rate": 0.0, "avg_latency_ms": 999999.0, "avg_retry_count": 999.0, "dangerous_non_idempotent_retries": 999.0, "useless_retries": 999.0, "good_fail_fast_decisions": 0.0, "good_endpoint_switches": 0.0, "combined_score": -1000000000.0}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: -1000000000.0000\n- Feature coordinates: combined_score=-1000000000.00, avg_retry_count=999.00\n- Focus areas: - Fitness declined: -4.3125 \u2192 -1000000000.0000. Consider revising recent changes.\n- Exploring combined_score=-1000000000.00, avg_retry_count=999.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4062, avg_latency_ms: 1171.8750, avg_retry_count: 2.0625, dangerous_non_idempotent_retries: 0.0000, useless_retries: 0.6875, good_fail_fast_decisions: 0.2812, good_endpoint_switches: 0.0000, combined_score: -4.3125\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4062, avg_latency_ms: 1171.8750, avg_retry_count: 2.0625, dangerous_non_idempotent_retries: 0.0000, useless_retries: 0.6875, good_fail_fast_decisions: 0.2812, good_endpoint_switches: 0.0000, combined_score: -4.3125\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4062, avg_latency_ms: 1171.8750, avg_retry_count: 2.0625, dangerous_non_idempotent_retries: 0.0000, useless_retries: 0.6875, good_fail_fast_decisions: 0.2812, good_endpoint_switches: 0.0000, combined_score: -4.3125\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: -4.3125)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall time limit - prevent runaway operations\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Exponential backoff with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(400 * (2 ** attempt), 6_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(200 * (2 ** attempt), 3_500)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n    else:\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n\n    # RTT-based adjustment - only add delay for very high latency\n    if last_rtt_ms > 2_500:\n        wait_ms = min(wait_ms + 150, 6_150)\n    elif last_rtt_ms > 1_500:\n        # Small adjustment for moderate latency\n        wait_ms = min(wait_ms + 50, 1_650)\n\n    # Total attempts limit - prevent infinite loops\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4062), Performs well on avg_latency_ms (1171.8750), Performs well on avg_retry_count (2.0625), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (0.6875), Performs well on good_fail_fast_decisions (0.2812), Performs well on good_endpoint_switches (0.0000), Performs well on combined_score (-4.3125)\n\n### Program 2 (Score: -4.3125)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall time limit check - prevent runaway operations\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Total attempts limit - prevent infinite loops\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        # More aggressive rate limiting - shorter waits\n        wait_ms = min(400 * (2 ** attempt), 6_000)\n    elif error_type == \"server_busy\":\n        # Moderate wait for server_busy errors\n        wait_ms = min(200 * (2 ** attempt), 3_500)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        # Slightly shorter waits for network-related errors\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n    else:\n        # Default for other error types\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n\n    # RTT-based adjustment - only add delay for very high latency\n    if last_rtt_ms > 2_500:\n        wait_ms = min(wait_ms + 150, 6_150)\n    elif last_rtt_ms > 1_500:\n        # Small adjustment for moderate latency\n        wait_ms = min(wait_ms + 50, 1_650)\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4062), Performs well on avg_latency_ms (1171.8750), Performs well on avg_retry_count (2.0625), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (0.6875), Performs well on good_fail_fast_decisions (0.2812), Performs well on good_endpoint_switches (0.0000), Performs well on combined_score (-4.3125)\n\n### Program 3 (Score: -4.3125)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall time limit - prevent runaway operations (critical for avoiding 999 retry count)\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Total attempts limit - prevent infinite loops\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(400 * (2 ** attempt), 6_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(200 * (2 ** attempt), 3_500)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n    else:\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n\n    # RTT-based adjustment - only add delay for very high latency\n    if last_rtt_ms > 2_500:\n        wait_ms = min(wait_ms + 150, 6_150)\n    elif last_rtt_ms > 1_500:\n        # Small adjustment for moderate latency\n        wait_ms = min(wait_ms + 50, 1_650)\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4062), Performs well on avg_latency_ms (1171.8750), Performs well on avg_retry_count (2.0625), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (0.6875), Performs well on good_fail_fast_decisions (0.2812), Performs well on good_endpoint_switches (0.0000), Performs well on combined_score (-4.3125)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: -4.3125)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall time limit check - prevent runaway operations\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - more aggressive than baseline\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 4_000}\n\n    # Total attempts limit - prevent infinite loops\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(400 * (2 ** attempt), 6_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(200 * (2 ** attempt), 3_500)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n    else:\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n\n    # RTT-based adjustment - only add delay for very high latency\n    if last_rtt_ms > 2_500:\n        wait_ms = min(wait_ms + 150, 6_150)\n    elif last_rtt_ms > 1_500:\n        # Small adjustment for moderate latency\n        wait_ms = min(wait_ms + 50, 1_650)\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n```\nKey features: Alternative approach to runs_successfully, Alternative approach to success_rate\n\n### Program D2 (Score: -4.3125)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall time limit - prevent runaway operations (critical for avoiding 999 retry count)\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Total attempts limit - prevent infinite loops (critical safeguard)\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(400 * (2 ** attempt), 6_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(200 * (2 ** attempt), 3_500)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n    else:\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n\n    # RTT-based adjustment - only add delay for very high latency\n    if last_rtt_ms > 2_500:\n        wait_ms = min(wait_ms + 150, 6_150)\n    elif last_rtt_ms > 1_500:\n        # Small adjustment for moderate latency\n        wait_ms = min(wait_ms + 50, 1_650)\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n```\nKey features: Alternative approach to runs_successfully, Alternative approach to success_rate\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: -15.1719, Type: Exploratory)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall timeout check - fail if too much time elapsed\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - more aggressive to reduce retries\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 4_000}\n\n    # Total attempts limit - conservative to reduce avg_retry_count\n    if attempt >= 3:\n        # For idempotent operations, try endpoint switch instead of failing\n        if is_idempotent:\n            return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(400 * (2 ** attempt), 7_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(250 * (2 ** attempt), 4_000)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n    else:\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n\n    # RTT-based adjustments - moderate increase for high latency\n    if last_rtt_ms > 1_500:\n        wait_ms = min(wait_ms + 100, 3_000)\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: -1000000000.0000, Type: Exploratory)\n```python\nLooking at the current program and evolution history, I can see the fitness has declined significantly to -18.3375, likely due to the increased time limit (15_000ms vs 10_000ms in successful programs) and the endpoint switching logic which may not be working well.\n\nThe top performing programs all achieve around -4.3125 with avg_retry_count=2.0625. Key elements that work well:\n- 10_000ms time limit (not 15_000ms)\n- circuit_open at consecutive_failures >= 5 (not 4)\n- Proper handling of different error types\n- RTT adjustments with thresholds at 2_500ms and 1_500ms\n\nLet me create a program that combines the proven elements with some diversity in retry behavior to explore better performance.\n\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall time limit check - prevent runaway operations (critical for avoiding 999 retry count)\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Total attempts limit - prevent infinite loops\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(400 * (2 ** attempt), 6_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(200 * (2 ** attempt), 3_500)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n    elif error_type == \"bad_gateway\":\n        wait_ms = min(150 * (2 ** attempt), 2_500)\n    elif error_type == \"temporary_disconnect\":\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n    else:  # slow_response and others\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n\n    # RTT-based adjustment - only add delay for very high latency\n    if last_rtt_ms > 2_500:\n        wait_ms = min(wait_ms\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: -1000000000.0000, Type: Exploratory)\n```python\nLooking at the current situation, I can see that the fitness score has crashed to -1,000,000,000 with avg_retry_count=999.00, indicating an infinite loop or unbounded retry behavior. The top performers consistently achieve -4.3125 with avg_retry_count=2.06.\n\nThe key issues are:\n1. The code appears to be incomplete/cut off, causing unbounded retries\n2. Missing proper termination conditions\n3. Need to ensure all code paths return valid actions\n\nI'll create a robust solution that:\n- Maintains the successful patterns from top performers\n- Adds proper safety checks to prevent infinite loops\n- Ensures all code paths return valid actions\n- Uses conservative retry limits to keep avg_retry_count low\n\n```python\n# Your rewritten program here\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Safety checks first - prevent infinite loops and runaway behavior\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall time limit - prevent runaway operations (critical for avoiding 999 retry count)\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Total attempts limit - prevent infinite loops\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(400 * (2 ** attempt), 6_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(200 * (2 ** attempt), 3_500)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n    else:\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n\n    # RTT-based adjustment - only add delay for very high latency\n    if last_rtt_ms > 2_500:\n        wait_ms = min(wait_ms + 150, 6_150)\n    elif last_rtt_ms > 1_500:\n        # Small adjustment for moderate latency\n        wait_ms = min(wait_ms + 50, 1_650)\n\n    # Ensure we always return a valid action - prevent infinite loops\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-B\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\nLooking at the current situation, I can see that the fitness score has collapsed to -1,000,000,000.0000 with avg_retry_count=999.00, indicating the program is likely stuck in an infinite loop or excessive retry behavior. The top performing programs all have consistent patterns that work well.\n\nLet me analyze what's happening and create a program that:\n1. Fixes the infinite loop issue that's causing the 999 retry count\n2. Maintains the successful patterns from the top performers\n3. Introduces diversity in retry behavior while keeping fitness high\n\nKey issues to address:\n- Missing final return statement causing undefined behavior\n- Not properly handling all code paths\n- Missing specific error type handling that's working well in top performers\n\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall time limit - prevent runaway operations\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 4_000}\n\n    # Total attempts limit - prevent infinite loops\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(400 * (2 ** attempt), 6_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(200 * (2 ** attempt), 3_500)\n    elif error_type == \"timeout\":\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n    elif error_type == \"connection_reset\":\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n    elif error_type == \"bad_gateway\":\n        wait_ms = min(150 * (2 ** attempt), 3_000)\n    elif error_type == \"temporary_disconnect\":\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n    elif error_type == \"slow_response\":\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n    else:\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n\n    # RTT-based adjustment - only add delay for very high latency\n    if last_rtt_ms\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: combined_score, avg_retry_count\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["Looking at the current situation, I can see the program has crashed to -1,000,000,000.0000 fitness with avg_retry_count=999.00, indicating an infinite loop or unbounded retry behavior. This is likely caused by an incomplete RTT-based adjustment logic that leaves the function without a proper return statement in some code paths.\n\nThe top performers consistently achieve -4.3125 with avg_retry_count=2.0625 by:\n1. Maintaining strict time limits (10,000ms)\n2. Using circuit breaker at consecutive_failures >= 5\n3. Properly handling all error types with specific strategies\n4. Ensuring all code paths return valid actions\n5. Keeping RTT adjustments conservative\n\nLet me create a robust solution that fixes the infinite loop issue while maintaining the successful patterns:\n\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Safety checks first - prevent infinite loops and runaway behavior\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall time limit - prevent runaway operations\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Total attempts limit - prevent infinite loops\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(400 * (2 ** attempt), 6_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(200 * (2 ** attempt), 3_500)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n    elif error_type == \"bad_gateway\":\n        wait_ms = min(150 * (2 ** attempt), 2_500)\n    elif error_type == \"temporary_disconnect\":\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n    elif error_type == \"slow_response\":\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n    else:\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n\n    # RTT-based adjustment - only add delay for very high latency\n    if last_rtt_ms > 2_50"]}}, "artifacts_json": null, "artifact_dir": null, "embedding": null}
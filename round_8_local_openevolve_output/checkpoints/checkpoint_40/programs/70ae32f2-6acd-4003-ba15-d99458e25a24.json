{"id": "70ae32f2-6acd-4003-ba15-d99458e25a24", "code": "Looking at the current program state and evolution history, I can see that the fitness score has declined dramatically to -1,000,000,000 with avg_retry_count=999, indicating the program is likely stuck in an infinite loop or failing catastrophically.\n\nThe top performers all achieve -4.3125 with avg_retry_count=2.06, showing consistent, well-behaved retry behavior. The key issues with the current program are:\n\n1. Missing proper timeout handling that could cause infinite loops\n2. Missing circuit breaker implementation\n3. Incomplete RTT-based adjustments (code got cut off)\n4. The 999 retry count suggests it's not properly limiting retries\n\nI'll create a program that:\n- Maintains the proven working configuration from top performers\n- Adds proper safety checks to prevent infinite loops\n- Includes complete error-type handling\n- Keeps diversity by slightly adjusting thresholds\n\n```python\n# Your rewritten program here\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Critical safety: always fail if circuit is open\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall time limit - prevent runaway operations (critical for avoiding 999 retry count)\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 4_000}\n\n    # Total attempts limit - prevent infinite loops\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(400 * (2 ** attempt), 6_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(200 * (2 ** attempt), 3_500)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n    elif error_type == \"bad_gateway\":\n        wait_ms = min(150 * (2 ** attempt), 3_000)\n    elif error_type == \"temporary_disconnect\":\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n    elif error_type == \"slow_response\":\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n    else:\n        # Fallback for unknown error types\n        wait_ms = min(70 * (2 ** attempt),", "changes_description": null, "language": "python", "parent_id": "243aad99-bec6-4cff-bd13-88c6a287fab7", "generation": 2, "timestamp": 1776877176.924031, "iteration_found": 36, "metrics": {"runs_successfully": 0.0, "success_rate": 0.0, "avg_latency_ms": 999999.0, "avg_retry_count": 999.0, "dangerous_non_idempotent_retries": 999.0, "useless_retries": 999.0, "good_fail_fast_decisions": 0.0, "good_endpoint_switches": 0.0, "combined_score": -1000000000.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"runs_successfully": 0.0, "success_rate": 0.0, "avg_latency_ms": 999999.0, "avg_retry_count": 999.0, "dangerous_non_idempotent_retries": 999.0, "useless_retries": 999.0, "good_fail_fast_decisions": 0.0, "good_endpoint_switches": 0.0, "combined_score": -1000000000.0}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: -1000000000.0000\n- Feature coordinates: combined_score=-1000000000.00, avg_retry_count=999.00\n- Focus areas: - Fitness declined: -4.3125 \u2192 -1000000000.0000. Consider revising recent changes.\n- Exploring combined_score=-1000000000.00, avg_retry_count=999.00 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4062, avg_latency_ms: 1171.8750, avg_retry_count: 2.0625, dangerous_non_idempotent_retries: 0.0000, useless_retries: 0.6875, good_fail_fast_decisions: 0.2812, good_endpoint_switches: 0.0000, combined_score: -4.3125\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4062, avg_latency_ms: 1171.8750, avg_retry_count: 2.0625, dangerous_non_idempotent_retries: 0.0000, useless_retries: 0.6875, good_fail_fast_decisions: 0.2812, good_endpoint_switches: 0.0000, combined_score: -4.3125\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4062, avg_latency_ms: 1171.8750, avg_retry_count: 2.0625, dangerous_non_idempotent_retries: 0.0000, useless_retries: 0.6875, good_fail_fast_decisions: 0.2812, good_endpoint_switches: 0.0000, combined_score: -4.3125\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: -4.3125)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall time limit - prevent runaway operations\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Exponential backoff with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(400 * (2 ** attempt), 6_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(200 * (2 ** attempt), 3_500)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n    else:\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n\n    # RTT-based adjustment - only add delay for very high latency\n    if last_rtt_ms > 2_500:\n        wait_ms = min(wait_ms + 150, 6_150)\n    elif last_rtt_ms > 1_500:\n        # Small adjustment for moderate latency\n        wait_ms = min(wait_ms + 50, 1_650)\n\n    # Total attempts limit - prevent infinite loops\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4062), Performs well on avg_latency_ms (1171.8750), Performs well on avg_retry_count (2.0625), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (0.6875), Performs well on good_fail_fast_decisions (0.2812), Performs well on good_endpoint_switches (0.0000), Performs well on combined_score (-4.3125)\n\n### Program 2 (Score: -4.3125)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall time limit check - prevent runaway operations\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Total attempts limit - prevent infinite loops\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        # More aggressive rate limiting - shorter waits\n        wait_ms = min(400 * (2 ** attempt), 6_000)\n    elif error_type == \"server_busy\":\n        # Moderate wait for server_busy errors\n        wait_ms = min(200 * (2 ** attempt), 3_500)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        # Slightly shorter waits for network-related errors\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n    else:\n        # Default for other error types\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n\n    # RTT-based adjustment - only add delay for very high latency\n    if last_rtt_ms > 2_500:\n        wait_ms = min(wait_ms + 150, 6_150)\n    elif last_rtt_ms > 1_500:\n        # Small adjustment for moderate latency\n        wait_ms = min(wait_ms + 50, 1_650)\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4062), Performs well on avg_latency_ms (1171.8750), Performs well on avg_retry_count (2.0625), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (0.6875), Performs well on good_fail_fast_decisions (0.2812), Performs well on good_endpoint_switches (0.0000), Performs well on combined_score (-4.3125)\n\n### Program 3 (Score: -4.3125)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall time limit - prevent runaway operations (critical for avoiding 999 retry count)\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Total attempts limit - prevent infinite loops\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(400 * (2 ** attempt), 6_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(200 * (2 ** attempt), 3_500)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n    else:\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n\n    # RTT-based adjustment - only add delay for very high latency\n    if last_rtt_ms > 2_500:\n        wait_ms = min(wait_ms + 150, 6_150)\n    elif last_rtt_ms > 1_500:\n        # Small adjustment for moderate latency\n        wait_ms = min(wait_ms + 50, 1_650)\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4062), Performs well on avg_latency_ms (1171.8750), Performs well on avg_retry_count (2.0625), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (0.6875), Performs well on good_fail_fast_decisions (0.2812), Performs well on good_endpoint_switches (0.0000), Performs well on combined_score (-4.3125)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: -4.3125)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall time limit check - prevent runaway operations\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - more aggressive than baseline\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 4_000}\n\n    # Total attempts limit - prevent infinite loops\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(400 * (2 ** attempt), 6_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(200 * (2 ** attempt), 3_500)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n    else:\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n\n    # RTT-based adjustment - only add delay for very high latency\n    if last_rtt_ms > 2_500:\n        wait_ms = min(wait_ms + 150, 6_150)\n    elif last_rtt_ms > 1_500:\n        # Small adjustment for moderate latency\n        wait_ms = min(wait_ms + 50, 1_650)\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n```\nKey features: Alternative approach to runs_successfully, Alternative approach to success_rate\n\n### Program D2 (Score: -5.5125)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall timeout check - moderate limit to balance latency and success\n    if elapsed_ms > 15_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Total attempts limit with idempotency consideration\n    if is_idempotent and attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n    elif not is_idempotent and attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(400 * (2 ** attempt), 6_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(200 * (2 ** attempt), 3_500)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n    else:\n        wait_ms = min(80 * (2 ** attempt), 1_600)\n\n    # RTT-based adjustment - moderate delay increase for high latency\n    if last_rtt_ms > 2_000:\n        wait_ms = min(wait_ms + 200, 6_200)\n    elif last_rtt_ms > 1_000:\n        wait_ms = min(wait_ms + 50, 1_650)\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n```\nKey features: Alternative approach to runs_successfully, Alternative approach to success_rate\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: -1000000000.0000, Type: Exploratory)\n```python\nLooking at the current program and the evolution history, I can see that the fitness score has declined from -4.3125 (the top performers) to -5.5125. The key differences between the top performers and the current program are:\n\n1. **Time limit**: Current uses 15,000ms vs top performers' 10,000ms\n2. **Consecutive failures threshold**: Current uses >=5 vs top performers' >=4\n3. **RTT thresholds**: Current uses 2,000ms/1,000ms vs top performers' 2,500ms/1,500ms\n4. **Error-specific handling**: Current lumps timeout/connection_reset together with lower multipliers\n5. **RTT adjustments**: Current adds less delay (200/50 vs 150/50)\n\nThe top performers all achieve -4.3125 with consistent metrics, suggesting this configuration is optimal. I'll revert to the proven configuration while maintaining some diversity in the retry behavior.\n\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall time limit check - prevent runaway operations (critical for avoiding 999 retry count)\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 4_000}\n\n    # Total attempts limit - prevent infinite loops\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        # More aggressive rate limiting - shorter waits\n        wait_ms = min(400 * (2 ** attempt), 6_000)\n    elif error_type == \"server_busy\":\n        # Moderate wait for server_busy errors\n        wait_ms = min(200 * (2 ** attempt), 3_500)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        # Slightly shorter waits for network-related errors\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n    else:\n        # Default for other error types\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n\n    # RTT-based adjustment - only add delay for very high latency\n    if last_rtt_ms > 2_50\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: -1000000000.0000, Type: Exploratory)\n```python\nLooking at the current program and evolution history, I can see that while it achieves a score of -8.6781, there are opportunities for improvement by incorporating strategies from better-performing programs in the inspiration set.\n\nKey insights from analyzing the programs:\n1. The current program has a relatively high `avg_retry_count` (2.03) and `combined_score` (-8.68)\n2. Programs that check `elapsed_ms` overall timeout tend to perform better\n3. Lowering the consecutive failures threshold (from 5 to 4) improves performance\n4. Adding endpoint switching for idempotent operations can help\n5. More specific error type handling with adjusted multipliers helps\n\nHere's my improved version that incorporates these insights while maintaining diversity:\n\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall timeout check - fail if too much time elapsed\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit earlier than baseline\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 4_000}\n\n    # Limit total attempts - more aggressive for non-idempotent operations\n    if is_idempotent:\n        if attempt >= 4:\n            return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n    else:\n        if attempt >= 2:\n            return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(400 * (2 ** attempt), 7_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(200 * (2 ** attempt), 3_500)\n    elif error_type == \"timeout\":\n        wait_ms = min(120 * (2 ** attempt), 2_500)\n    elif error_type == \"connection_reset\":\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n    elif error_type == \"bad_gateway\":\n        wait_ms = min(150 * (2 ** attempt), 3_000)\n    elif error_type == \"temporary_disconnect\":\n        wait_ms = min(80 * (2 ** attempt), 1_600)\n    elif error_type == \"slow_response\":\n        wait_ms = min(60 * (2 ** attempt), 1_200)\n    else:\n        wait_ms = min(100\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: -10.0844, Type: Exploratory)\n```python\n# Your rewritten program here\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Critical safety: always fail if circuit is open\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall timeout check - fail if too much time elapsed\n    if elapsed_ms > 12_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Limit total attempts\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(500 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(250 * (2 ** attempt), 4_000)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        wait_ms = min(150 * (2 ** attempt), 3_000)\n    else:\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n\n    # RTT-based adjustments - more sophisticated handling\n    if last_rtt_ms > 2_000:\n        wait_ms = min(wait_ms + 250, 8_500)\n    \n    # High RTT with persistent failures suggests endpoint issues\n    if last_rtt_ms > 3_000 and consecutive_failures >= 2:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\nLooking at the current program and evolution history, I notice that:\n1. The current program has fitness -8.6781 with avg_retry_count=2.03\n2. The top performers achieve -4.3125 with avg_retry_count=2.06\n3. The current program is missing key optimizations that improved fitness in previous attempts\n\nKey issues with the current program:\n- Missing `elapsed_ms` timeout check (used in top performers)\n- Less aggressive circuit breaking (using 5 vs 4 consecutive failures)\n- Missing specific handling for different error types\n- Missing RTT-based cap on wait time\n\nI'll create a program that combines the best elements from top performers while maintaining diversity:\n\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Circuit breaker logic first - critical for preventing infinite loops\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall time limit check - prevent runaway operations\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - more aggressive than baseline\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 4_000}\n\n    # Total attempts limit - prevent infinite loops\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(400 * (2 ** attempt), 6_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(200 * (2 ** attempt), 3_500)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n    elif error_type == \"bad_gateway\":\n        wait_ms = min(150 * (2 ** attempt), 3_000)\n    elif error_type == \"temporary_disconnect\":\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n    elif error_type == \"slow_response\":\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n    else:\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n\n    # RTT-based adjustment - only add delay for very high latency\n    if last_rtt_ms > 2_500:\n        wait_ms = min(wait_ms + 150, 6_150)\n    elif last_rtt_ms > 1_500:\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: combined_score, avg_retry_count\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["Looking at the current program state and evolution history, I can see that the fitness score has declined dramatically to -1,000,000,000 with avg_retry_count=999, indicating the program is likely stuck in an infinite loop or failing catastrophically.\n\nThe top performers all achieve -4.3125 with avg_retry_count=2.06, showing consistent, well-behaved retry behavior. The key issues with the current program are:\n\n1. Missing proper timeout handling that could cause infinite loops\n2. Missing circuit breaker implementation\n3. Incomplete RTT-based adjustments (code got cut off)\n4. The 999 retry count suggests it's not properly limiting retries\n\nI'll create a program that:\n- Maintains the proven working configuration from top performers\n- Adds proper safety checks to prevent infinite loops\n- Includes complete error-type handling\n- Keeps diversity by slightly adjusting thresholds\n\n```python\n# Your rewritten program here\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    # Critical safety: always fail if circuit is open\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-retryable errors always fail immediately\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Non-idempotent operations: fail after first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Overall time limit - prevent runaway operations (critical for avoiding 999 retry count)\n    if elapsed_ms > 10_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Consecutive failures threshold - open circuit before hard limit\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 4_000}\n\n    # Total attempts limit - prevent infinite loops\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Calculate base wait time with error-type specific strategies\n    if error_type == \"rate_limit\":\n        wait_ms = min(400 * (2 ** attempt), 6_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(200 * (2 ** attempt), 3_500)\n    elif error_type in {\"timeout\", \"connection_reset\"}:\n        wait_ms = min(80 * (2 ** attempt), 1_800)\n    elif error_type == \"bad_gateway\":\n        wait_ms = min(150 * (2 ** attempt), 3_000)\n    elif error_type == \"temporary_disconnect\":\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n    elif error_type == \"slow_response\":\n        wait_ms = min(70 * (2 ** attempt), 1_600)\n    else:\n        # Fallback for unknown error types\n        wait_ms = min(70 * (2 ** attempt),"]}}, "artifacts_json": null, "artifact_dir": null, "embedding": null}
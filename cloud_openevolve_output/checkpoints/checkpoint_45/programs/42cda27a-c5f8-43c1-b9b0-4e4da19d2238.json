{"id": "42cda27a-c5f8-43c1-b9b0-4e4da19d2238", "code": "\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    if error_type in {\"bad_gateway\", \"connection_reset\"} and attempt >= 1:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if last_rtt_ms > 4_000:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    if error_type == \"rate_limit\":\n        wait_ms = min(800 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(400 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(150 * (2 ** attempt), 2_000)\n\n    if last_rtt_ms > 1_500:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n", "changes_description": null, "language": "python", "parent_id": "4a61a3b5-54f7-4843-9ee4-d38a92f6c93e", "generation": 2, "timestamp": 1776744411.92067, "iteration_found": 4, "metrics": {"runs_successfully": 1.0, "success_rate": 0.4375, "avg_latency_ms": 1484.0625, "avg_retry_count": 1.96875, "dangerous_non_idempotent_retries": 0.0, "useless_retries": 0.75, "good_fail_fast_decisions": 0.28125, "good_endpoint_switches": 0.03125, "combined_score": -10.7875}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  if consecutive_failures >= 5:\n          return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n  \n      if consecutive_failures >= 3 or error_type in {\"bad_gateway\", \"connection_reset\"}:\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      if attempt >= 3:\n          return {\"action\": \"fail\", \"wait_ms\": 0}\n  \n      if error_type == \"rate_limit\":\n          wait_ms = min(1000 * (2 ** attempt), 10_000)\n      elif error_type == \"server_busy\":\n          wait_ms = min(500 * (2 ** attempt), 5_000)\n      else:\n          wait_ms = min(200 * (2 ** attempt), 3_000)\n  \n      if last_rtt_ms > 3_000:\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      return {\"action\": \"retry\", \"wait_ms\": wait_ms}\nwith:\n  if consecutive_failures >= 5:\n          return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n  \n      if error_type in {\"bad_gateway\", \"connection_reset\"} and attempt >= 1:\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      if attempt >= 3:\n          return {\"action\": \"fail\", \"wait_ms\": 0}\n  \n      if last_rtt_ms > 4_000:\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      if error_type == \"rate_limit\":\n          wait_ms = min(800 * (2 ** attempt), 8_000)\n      elif error_type == \"server_busy\":\n          wait_ms = min(400 * (2 ** attempt), 4_000)\n      else:\n          wait_ms = min(150 * (2 ** attempt), 2_000)\n  \n      if last_rtt_ms > 1_500:\n          wait_ms += 300\n  \n      return {\"action\": \"retry\", \"wait_ms\": wait_ms}", "parent_metrics": {"runs_successfully": 1.0, "success_rate": 0.46875, "avg_latency_ms": 1421.875, "avg_retry_count": 2.09375, "dangerous_non_idempotent_retries": 0.0, "useless_retries": 1.21875, "good_fail_fast_decisions": 0.15625, "good_endpoint_switches": 0.0625, "combined_score": -11.640625}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: -11.6406\n- Feature coordinates: combined_score=-11.64, avg_retry_count=2.09\n- Focus areas: - Fitness unchanged at -11.6406\n- Exploring combined_score=-11.64, avg_retry_count=2.09 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  if consecutive_failures >= 5:\n          return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n  \n      if attempt >= 3:\n          return {\"action\": \"fail\", \"wait_ms\": 0}\n  \n      if error_type == \"rate_limit\":\n          wait_ms = min(500 * (2 ** attempt), 8_000)\n      elif error_type == \"server_busy\":\n          wait_ms = min(250 * (2 ** attempt), 4_000)\n      else:\n          wait_ms = min(100 * (2 ** attempt), 2_000)\n  \n      if last_rtt_ms > 2_000:\n          wait_ms += 250\n  \n      return {\"action\": \"retry\", \"wait_ms\": wait_ms}\nwith:\n  if consecutive_failures >= 5:\n          return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n  \n      if consecutive_failures >= 3 or error_type in {\"bad_gateway\", \"connection_reset\"}:\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      if attempt >= 3:\n          return {\"action\": \"fail\", \"wait_ms\": 0}\n  \n      if error_type == \"rate_limit\":\n          wait_ms = min(1000 * (2 ** attempt), 10_000)\n      elif error_type == \"server_busy\":\n          wait_ms = min(500 * (2 ** attempt), 5_000)\n      else:\n          wait_ms = min(200 * (2 ** attempt), 3_000)\n  \n      if last_rtt_ms > 3_000:\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4688, avg_latency_ms: 1421.8750, avg_retry_count: 2.0938, dangerous_non_idempotent_retries: 0.0000, useless_retries: 1.2188, good_fail_fast_decisions: 0.1562, good_endpoint_switches: 0.0625, combined_score: -11.6406\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Unknown changes\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4062, avg_latency_ms: 1320.0000, avg_retry_count: 2.0312, dangerous_non_idempotent_retries: 0.0000, useless_retries: 0.6875, good_fail_fast_decisions: 0.2812, good_endpoint_switches: 0.0000, combined_score: -8.6781\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Change 1: Replace:\n  if consecutive_failures >= 5:\n          return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n  \n      if attempt >= 3:\n          return {\"action\": \"fail\", \"wait_ms\": 0}\n  \n      if error_type == \"rate_limit\":\n          wait_ms = min(500 * (2 ** attempt), 8_000)\n      elif error_type == \"server_busy\":\n          wait_ms = min(250 * (2 ** attempt), 4_000)\n      else:\n          wait_ms = min(100 * (2 ** attempt), 2_000)\n  \n      if last_rtt_ms > 2_000:\n          wait_ms += 250\n  \n      return {\"action\": \"retry\", \"wait_ms\": wait_ms}\nwith:\n  if consecutive_failures >= 5:\n          return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n  \n      if consecutive_failures >= 2 and error_type in {\"bad_gateway\", \"timeout\"}:\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      if attempt >= 3:\n          return {\"action\": \"fail\", \"wait_ms\": 0}\n  \n      if last_rtt_ms > 5_000:\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      if error_type == \"rate_limit\":\n          wait_ms = min(500 * (2 ** attempt), 8_000)\n      elif error_type == \"server_busy\":\n          wait_ms = min(250 * (2 ** attempt), 4_000)\n      else:\n          wait_ms = min(100 * (2 ** attempt), 2_000)\n  \n      if last_rtt_ms > 2_000:\n          wait_ms += 250\n  \n      return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4375, avg_latency_ms: 1207.5000, avg_retry_count: 2.0312, dangerous_non_idempotent_retries: 0.0000, useless_retries: 0.7188, good_fail_fast_decisions: 0.2812, good_endpoint_switches: 0.0312, combined_score: -2.3344\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: -2.3344)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    if consecutive_failures >= 2 and error_type in {\"bad_gateway\", \"timeout\"}:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if last_rtt_ms > 5_000:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    if error_type == \"rate_limit\":\n        wait_ms = min(500 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(250 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n\n    if last_rtt_ms > 2_000:\n        wait_ms += 250\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4375), Performs well on avg_latency_ms (1207.5000), Performs well on avg_retry_count (2.0312), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (0.7188), Performs well on good_fail_fast_decisions (0.2812), Performs well on good_endpoint_switches (0.0312), Performs well on combined_score (-2.3344)\n\n### Program 2 (Score: -8.6781)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type == \"rate_limit\":\n        wait_ms = min(500 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(250 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n\n    if last_rtt_ms > 2_000:\n        wait_ms += 250\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4062), Performs well on avg_latency_ms (1320.0000), Performs well on avg_retry_count (2.0312), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (0.6875), Performs well on good_fail_fast_decisions (0.2812), Performs well on good_endpoint_switches (0.0000), Performs well on combined_score (-8.6781)\n\n### Program 3 (Score: -11.6406)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    if consecutive_failures >= 3 or error_type in {\"bad_gateway\", \"connection_reset\"}:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type == \"rate_limit\":\n        wait_ms = min(1000 * (2 ** attempt), 10_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(500 * (2 ** attempt), 5_000)\n    else:\n        wait_ms = min(200 * (2 ** attempt), 3_000)\n\n    if last_rtt_ms > 3_000:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4688), Performs well on avg_latency_ms (1421.8750), Performs well on avg_retry_count (2.0938), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (1.2188), Performs well on good_fail_fast_decisions (0.1562), Performs well on good_endpoint_switches (0.0625), Performs well on combined_score (-11.6406)\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: -2.3344, Type: Exploratory)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    if consecutive_failures >= 2 and error_type in {\"bad_gateway\", \"timeout\"}:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if last_rtt_ms > 5_000:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    if error_type == \"rate_limit\":\n        wait_ms = min(500 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(250 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n\n    if last_rtt_ms > 2_000:\n        wait_ms += 250\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: -8.6781, Type: Exploratory)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type == \"rate_limit\":\n        wait_ms = min(500 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(250 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n\n    if last_rtt_ms > 2_000:\n        wait_ms += 250\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    if consecutive_failures >= 3 or error_type in {\"bad_gateway\", \"connection_reset\"}:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type == \"rate_limit\":\n        wait_ms = min(1000 * (2 ** attempt), 10_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(500 * (2 ** attempt), 5_000)\n    else:\n        wait_ms = min(200 * (2 ** attempt), 3_000)\n\n    if last_rtt_ms > 3_000:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: combined_score, avg_retry_count\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current program (Attempt 3) has a high success rate but suffers from a high number of \"useless retries\" and a low \"good fail-fast decision\" score. The recent attempt to increase wait times and switch endpoints more aggressively (on `consecutive_failures >= 3` or `last_rtt_ms > 3000`) seems to have backfired, likely because it's switching endpoints too frequently or waiting too long for errors that could be failed fast.\n\nTo improve the fitness score, I will:\n1.  **Reduce useless retries**: Revert the aggressive `switch_endpoint` on `consecutive_failures >= 3` to a more balanced approach.\n2.  **Improve fail-fast decisions**: Instead of switching endpoints on high RTT (which might just be a slow but working connection), I'll use a more moderate RTT threshold and prioritize `fail` for certain error types if they persist.\n3.  **Optimize wait times**: The current exponential backoff for `rate_limit` and `server_busy` is quite high. I will refine these to be more responsive.\n4.  **Target `good_endpoint_switches`**: I'll trigger a switch on specific critical errors (`bad_gateway`, `connection_reset`) earlier, but only if they are actually problematic, to balance success rate and latency.\n\nThe goal is to move back towards the logic of Program 1 (the top performer) while fine-tuning the thresholds.\n\n<<<<<<< SEARCH\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    if consecutive_failures >= 3 or error_type in {\"bad_gateway\", \"connection_reset\"}:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type == \"rate_limit\":\n        wait_ms = min(1000 * (2 ** attempt), 10_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(500 * (2 ** attempt), 5_000)\n    else:\n        wait_ms = min(200 * (2 ** attempt), 3_000)\n\n    if last_rtt_ms > 3_000:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n=======\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    if error_type in {\"bad_gateway\", \"connection_reset\"} and attempt >= 1:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if last_rtt_ms > 4_000:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    if error_type == \"rate_limit\":\n        wait_ms = min(800 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(400 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(150 * (2 ** attempt), 2_000)\n\n    if last_rtt_ms > 1_500:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n>>>>>>> REPLACE"]}}, "artifacts_json": null, "artifact_dir": null, "embedding": null}
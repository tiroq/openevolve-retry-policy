{"id": "4a489c0e-6fc1-493c-924c-0ac7ab5627f2", "code": "\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Switch endpoint on critical network errors or high latency\n    if error_type in {\"bad_gateway\", \"timeout\", \"connection_reset\", \"temporary_disconnect\"} or last_rtt_ms > 3_000:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    # Switch endpoint if we hit repeated congestion\n    if attempt >= 1 and error_type in {\"server_busy\", \"rate_limit\"} and consecutive_failures >= 2:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    # Fail fast if total elapsed time is too high or max attempts reached\n    if attempt >= 3 or elapsed_ms > 14_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type == \"rate_limit\":\n        wait_ms = min(600 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(400 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(250 * (2 ** attempt), 2_500)\n\n    # Add jitter/extra wait if RTT is creeping up\n    if last_rtt_ms > 1_500:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n", "changes_description": null, "language": "python", "parent_id": "42cda27a-c5f8-43c1-b9b0-4e4da19d2238", "generation": 3, "timestamp": 1776745223.576159, "iteration_found": 39, "metrics": {"runs_successfully": 1.0, "success_rate": 0.46875, "avg_latency_ms": 544.6875, "avg_retry_count": 2.09375, "dangerous_non_idempotent_retries": 0.0, "useless_retries": 1.6875, "good_fail_fast_decisions": 0.1875, "good_endpoint_switches": 0.125, "combined_score": 10.55}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Change 1: Replace:\n  if error_type in {\"bad_gateway\", \"connection_reset\"} and attempt >= 1:\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      if attempt >= 3:\n          return {\"action\": \"fail\", \"wait_ms\": 0}\n  \n      if last_rtt_ms > 4_000:\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      if error_type == \"rate_limit\":\n          wait_ms = min(800 * (2 ** attempt), 8_000)\n      elif error_type == \"server_busy\":\n          wait_ms = min(400 * (2 ** attempt), 4_000)\n      else:\n          wait_ms = min(150 * (2 ** attempt), 2_000)\n  \n      if last_rtt_ms > 1_500:\n          wait_ms += 300\n  \n      return {\"action\": \"retry\", \"wait_ms\": wait_ms}\nwith:\n  # Switch endpoint on critical network errors or high latency\n      if error_type in {\"bad_gateway\", \"timeout\", \"connection_reset\", \"temporary_disconnect\"} or la...\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      # Switch endpoint if we hit repeated congestion\n      if attempt >= 1 and error_type in {\"server_busy\", \"rate_limit\"} and consecutive_failures >= 2:\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      # Fail fast if total elapsed time is too high or max attempts reached\n      if attempt >= 3 or elapsed_ms > 14_000:\n          return {\"action\": \"fail\", \"wait_ms\": 0}\n  \n      if error_type == \"rate_limit\":\n          wait_ms = min(600 * (2 ** attempt), 8_000)\n      elif error_type == \"server_busy\":\n          wait_ms = min(400 * (2 ** attempt), 4_000)\n      else:\n          wait_ms = min(250 * (2 ** attempt), 2_500)\n  \n      # Add jitter/extra wait if RTT is creeping up\n      if last_rtt_ms > 1_500:\n          wait_ms += 300\n  \n      return {\"action\": \"retry\", \"wait_ms\": wait_ms}", "parent_metrics": {"runs_successfully": 1.0, "success_rate": 0.4375, "avg_latency_ms": 1484.0625, "avg_retry_count": 1.96875, "dangerous_non_idempotent_retries": 0.0, "useless_retries": 0.75, "good_fail_fast_decisions": 0.28125, "good_endpoint_switches": 0.03125, "combined_score": -10.7875}, "island": 0}, "prompts": {"diff_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: -10.7875\n- Feature coordinates: combined_score=-10.79, avg_retry_count=1.97\n- Focus areas: - Fitness declined: 10.8781 \u2192 -10.7875. Consider revising recent changes.\n- Exploring combined_score=-10.79, avg_retry_count=1.97 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Change 1: Replace:\n  if consecutive_failures >= 3 or error_type in {\"bad_gateway\", \"connection_reset\"}:\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      if attempt >= 3:\n          return {\"action\": \"fail\", \"wait_ms\": 0}\n  \n      if error_type == \"rate_limit\":\n          wait_ms = min(1000 * (2 ** attempt), 10_000)\n      elif error_type == \"server_busy\":\n          wait_ms = min(500 * (2 ** attempt), 5_000)\n      else:\n          wait_ms = min(200 * (2 ** attempt), 3_000)\n  \n      if last_rtt_ms > 3_000:\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      return {\"action\": \"retry\", \"wait_ms\": wait_ms}\nwith:\n  # Switch endpoint on critical network errors or high latency\n      if error_type in {\"bad_gateway\", \"timeout\", \"connection_reset\", \"temporary_disconnect\"} or la...\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      # Switch endpoint if we hit repeated congestion\n      if attempt >= 1 and error_type in {\"server_busy\", \"rate_limit\"} and consecutive_failures >= 2:\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      # Fail fast if total elapsed time is too high or max attempts reached\n      if attempt >= 3 or elapsed_ms > 15_000:\n          return {\"action\": \"fail\", \"wait_ms\": 0}\n  \n      if error_type == \"rate_limit\":\n          wait_ms = min(600 * (2 ** attempt), 8_000)\n      elif error_type == \"server_busy\":\n          wait_ms = min(400 * (2 ** attempt), 4_000)\n      else:\n          wait_ms = min(200 * (2 ** attempt), 2_500)\n  \n      # Add jitter/extra wait if RTT is creeping up\n      if last_rtt_ms > 2_000:\n          wait_ms += 300\n  \n      return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4688, avg_latency_ms: 533.7500, avg_retry_count: 2.0938, dangerous_non_idempotent_retries: 0.0000, useless_retries: 1.6875, good_fail_fast_decisions: 0.1875, good_endpoint_switches: 0.1250, combined_score: 10.8781\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Change 1: Replace:\n  # Switch endpoint on critical network errors or high latency\n      if error_type in {\"bad_gateway\", \"timeout\", \"connection_reset\", \"temporary_disconnect\"} or la...\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      # Switch endpoint if we hit repeated congestion\n      if attempt >= 1 and error_type in {\"server_busy\", \"rate_limit\"} and consecutive_failures >= 2:\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      # Fail fast if total elapsed time is too high or max attempts reached\n      if attempt >= 3 or elapsed_ms > 15_000:\n          return {\"action\": \"fail\", \"wait_ms\": 0}\nwith:\n  # Switch endpoint on critical network errors or extreme latency\n      if error_type in {\"bad_gateway\", \"timeout\", \"connection_reset\", \"temporary_disconnect\"} or la...\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      # Switch endpoint if we hit repeated congestion or persistent rate limits\n      if (attempt >= 1 and error_type in {\"server_busy\", \"rate_limit\"} and consecutive_failures >= ...\n         (error_type == \"rate_limit\" and attempt >= 2):\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      # Fail fast if total elapsed time is too high, max attempts reached, or extreme latency\n      if attempt >= 3 or elapsed_ms > 12_000 or last_rtt_ms > 5_000:\n          return {\"action\": \"fail\", \"wait_ms\": 0}\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4688, avg_latency_ms: 533.7500, avg_retry_count: 2.0938, dangerous_non_idempotent_retries: 0.0000, useless_retries: 1.6875, good_fail_fast_decisions: 0.1875, good_endpoint_switches: 0.1250, combined_score: 10.8781\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Change 1: Replace:\n  # Switch endpoint on critical network errors or high latency\n      if error_type in {\"bad_gateway\", \"timeout\", \"connection_reset\", \"temporary_disconnect\"} or la...\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      # Switch endpoint if we hit repeated congestion\n      if attempt >= 1 and error_type in {\"server_busy\", \"rate_limit\"} and consecutive_failures >= 2:\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      # Fail fast if total elapsed time is too high or max attempts reached\n      if attempt >= 3 or elapsed_ms > 15_000:\n          return {\"action\": \"fail\", \"wait_ms\": 0}\n  \n      if error_type == \"rate_limit\":\n          wait_ms = min(600 * (2 ** attempt), 8_000)\n      elif error_type == \"server_busy\":\n          wait_ms = min(400 * (2 ** attempt), 4_000)\n      else:\n          wait_ms = min(200 * (2 ** attempt), 2_500)\nwith:\n  # Switch endpoint on critical network errors or high latency\n      if error_type in {\"bad_gateway\", \"timeout\", \"connection_reset\", \"temporary_disconnect\"} or la...\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      # Switch endpoint if we hit repeated congestion\n      if attempt >= 1 and error_type in {\"server_busy\", \"rate_limit\"} and consecutive_failures >= 2:\n          return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n  \n      # Fail fast if total elapsed time is too high or max attempts reached\n      if attempt >= 3 or elapsed_ms > 13_000:\n          return {\"action\": \"fail\", \"wait_ms\": 0}\n  \n      if error_type == \"rate_limit\":\n          wait_ms = min(700 * (2 ** attempt), 8_000)\n      elif error_type == \"server_busy\":\n          wait_ms = min(500 * (2 ** attempt), 4_000)\n      else:\n          wait_ms = min(250 * (2 ** attempt), 2_500)\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4688, avg_latency_ms: 533.7500, avg_retry_count: 2.0938, dangerous_non_idempotent_retries: 0.0000, useless_retries: 1.6875, good_fail_fast_decisions: 0.1875, good_endpoint_switches: 0.1250, combined_score: 10.8781\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 10.8781)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Switch endpoint on critical network errors or high latency\n    if error_type in {\"bad_gateway\", \"timeout\", \"connection_reset\", \"temporary_disconnect\"} or last_rtt_ms > 3_000:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    # Switch endpoint if we hit repeated congestion\n    if attempt >= 1 and error_type in {\"server_busy\", \"rate_limit\"} and consecutive_failures >= 2:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    if attempt >= 3 or elapsed_ms > 15_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type == \"rate_limit\":\n        wait_ms = min(600 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(400 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(200 * (2 ** attempt), 2_500)\n\n    # Add jitter/extra wait if RTT is creeping up\n    if last_rtt_ms > 2_000:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4688), Performs well on avg_latency_ms (533.7500), Performs well on avg_retry_count (2.0938), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (1.6875), Performs well on good_fail_fast_decisions (0.1875), Performs well on good_endpoint_switches (0.1250), Performs well on combined_score (10.8781)\n\n### Program 2 (Score: 10.8781)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Switch endpoint on critical network errors or extreme latency\n    if error_type in {\"bad_gateway\", \"timeout\", \"connection_reset\", \"temporary_disconnect\"} or last_rtt_ms > 4_000:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    # Switch endpoint if we hit repeated congestion or persistent rate limits\n    if (attempt >= 1 and error_type in {\"server_busy\", \"rate_limit\"} and consecutive_failures >= 2) or \\\n       (error_type == \"rate_limit\" and attempt >= 2):\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    # Fail fast if total elapsed time is too high, max attempts reached, or extreme latency\n    if attempt >= 3 or elapsed_ms > 12_000 or last_rtt_ms > 5_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type == \"rate_limit\":\n        wait_ms = min(600 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(400 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(200 * (2 ** attempt), 2_500)\n\n    # Add jitter/extra wait if RTT is creeping up\n    if last_rtt_ms > 2_000:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4688), Performs well on avg_latency_ms (533.7500), Performs well on avg_retry_count (2.0938), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (1.6875), Performs well on good_fail_fast_decisions (0.1875), Performs well on good_endpoint_switches (0.1250), Performs well on combined_score (10.8781)\n\n### Program 3 (Score: 10.8781)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Switch endpoint on critical network errors or high latency\n    if error_type in {\"bad_gateway\", \"timeout\", \"connection_reset\", \"temporary_disconnect\"} or last_rtt_ms > 3_000:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    # Switch endpoint if we hit repeated congestion\n    if attempt >= 1 and error_type in {\"server_busy\", \"rate_limit\"} and consecutive_failures >= 2:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    # Fail fast if total elapsed time is too high or max attempts reached\n    if attempt >= 3 or elapsed_ms > 15_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type == \"rate_limit\":\n        wait_ms = min(600 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(400 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(200 * (2 ** attempt), 2_500)\n\n    # Add jitter/extra wait if RTT is creeping up\n    if last_rtt_ms > 2_000:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4688), Performs well on avg_latency_ms (533.7500), Performs well on avg_retry_count (2.0938), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (1.6875), Performs well on good_fail_fast_decisions (0.1875), Performs well on good_endpoint_switches (0.1250), Performs well on combined_score (10.8781)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 10.8781)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Switch endpoint on critical network errors or high latency\n    if error_type in {\"bad_gateway\", \"timeout\", \"connection_reset\", \"temporary_disconnect\"} or last_rtt_ms > 3_000:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n    \n    # Switch endpoint if we hit repeated congestion\n    if attempt >= 1 and error_type in {\"server_busy\", \"rate_limit\"} and consecutive_failures >= 2:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    # Fail fast if total elapsed time is too high or max attempts reached\n    if attempt >= 3 or elapsed_ms > 15_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type == \"rate_limit\":\n        wait_ms = min(600 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(400 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(200 * (2 ** attempt), 2_500)\n\n    # Add jitter/extra wait if RTT is creeping up\n    if last_rtt_ms > 2_000:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n\n```\nKey features: Alternative approach to runs_successfully, Alternative approach to success_rate\n\n### Program D2 (Score: 10.8781)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Switch endpoint on critical network errors, extreme latency, or repeated congestion\n    if error_type in {\"bad_gateway\", \"timeout\", \"connection_reset\", \"temporary_disconnect\"} or last_rtt_ms > 3_500:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    if attempt >= 1 and error_type in {\"server_busy\", \"rate_limit\"} and consecutive_failures >= 2:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    # Fail fast if total elapsed time is too high or max attempts reached\n    if elapsed_ms > 15_000 or attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type == \"rate_limit\":\n        wait_ms = min(600 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(400 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(200 * (2 ** attempt), 2_500)\n\n    # Add jitter/extra wait if RTT is creeping up\n    if last_rtt_ms > 2_000:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n\n```\nKey features: Alternative approach to runs_successfully, Alternative approach to success_rate\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: 8.0656, Type: High-Performer)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Switch endpoint on critical network errors or moderate latency degradation\n    if error_type in {\"bad_gateway\", \"timeout\", \"connection_reset\", \"temporary_disconnect\"} or last_rtt_ms > 2_500:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    # Switch endpoint if we hit repeated congestion or persistent rate limits\n    if (attempt >= 1 and error_type in {\"server_busy\", \"rate_limit\"} and consecutive_failures >= 2) or \\\n       (error_type == \"rate_limit\" and attempt >= 2):\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    # Fail fast if total elapsed time is too high or max attempts reached\n    if attempt >= 3 or elapsed_ms > 14_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type == \"rate_limit\":\n        wait_ms = min(800 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(400 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(200 * (2 ** attempt), 2_500)\n\n    # Add jitter/extra wait if RTT is creeping up\n    if last_rtt_ms > 2_000:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 10.8781, Type: High-Performer)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Switch endpoint on critical network errors or high latency\n    if error_type in {\"bad_gateway\", \"timeout\", \"connection_reset\", \"temporary_disconnect\"} or last_rtt_ms > 3_500:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    # Switch endpoint if we hit repeated congestion or persistent rate limits\n    if (attempt >= 1 and error_type in {\"server_busy\", \"rate_limit\"} and consecutive_failures >= 2) or \\\n       (error_type == \"rate_limit\" and attempt >= 2):\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    # Fail fast if total elapsed time is too high or max attempts reached\n    if attempt >= 3 or elapsed_ms > 15_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type == \"rate_limit\":\n        wait_ms = min(600 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(400 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(200 * (2 ** attempt), 2_500)\n\n    # Add jitter/extra wait if RTT is creeping up\n    if last_rtt_ms > 2_000:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: -8.6781, Type: Exploratory)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type == \"rate_limit\":\n        wait_ms = min(500 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(250 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(100 * (2 ** attempt), 2_000)\n\n    if last_rtt_ms > 2_000:\n        wait_ms += 250\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n\n```\nUnique approach: [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    if error_type in {\"bad_gateway\", \"connection_reset\"} and attempt >= 1:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if last_rtt_ms > 4_000:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    if error_type == \"rate_limit\":\n        wait_ms = min(800 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(400 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(150 * (2 ** attempt), 2_000)\n\n    if last_rtt_ms > 1_500:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n\n```\n\n# Task\nSuggest improvements to the program that will improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: combined_score, avg_retry_count\nDifferent solutions with similar fitness but different features are valuable.\n\nYou MUST use the exact SEARCH/REPLACE diff format shown below to indicate changes:\n\n<<<<<<< SEARCH\n# Original code to find and replace (must match exactly)\n=======\n# New replacement code\n>>>>>>> REPLACE\n\nExample of valid diff format:\n<<<<<<< SEARCH\nfor i in range(m):\n    for j in range(p):\n        for k in range(n):\n            C[i, j] += A[i, k] * B[k, j]\n=======\n# Reorder loops for better memory access pattern\nfor i in range(m):\n    for k in range(n):\n        for j in range(p):\n            C[i, j] += A[i, k] * B[k, j]\n>>>>>>> REPLACE\n\nYou can suggest multiple changes. Each SEARCH section must exactly match code in the current program.\nBe thoughtful about your changes and explain your reasoning thoroughly.\n\nIMPORTANT: Do not rewrite the entire program - focus on targeted improvements.", "responses": ["The current program's fitness declined because it became too aggressive in failing or switching endpoints too early, or it didn't handle specific congestion patterns effectively. Specifically, the `error_type in {\"bad_gateway\", \"connection_reset\"} and attempt >= 1` logic might be switching endpoints too frequently for transient errors that could be resolved with a simple retry, and the `last_rtt_ms > 4_000` threshold for switching is quite high, potentially allowing too much latency to accumulate before acting.\n\nTo improve the score, I will:\n1.  **Refine Endpoint Switching**: Instead of switching on any `bad_gateway` after attempt 1, I'll group critical network errors (like `timeout` and `connection_reset`) and switch more decisively if they persist, but allow a single retry for others to avoid unnecessary switches.\n2.  **Optimize Congestion Handling**: I will implement a more robust check for `rate_limit` and `server_busy`. If these occur multiple times consecutively, we should switch endpoints rather than just waiting, as the current endpoint is clearly under pressure.\n3.  **Balance Fail-Fast vs. Success Rate**: I'll adjust the `elapsed_ms` and `attempt` limits to be slightly more generous to improve the `success_rate`, while keeping the `last_rtt_ms` check tighter to prevent high latency from dragging down the average.\n\n<<<<<<< SEARCH\n    if error_type in {\"bad_gateway\", \"connection_reset\"} and attempt >= 1:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if last_rtt_ms > 4_000:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    if error_type == \"rate_limit\":\n        wait_ms = min(800 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(400 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(150 * (2 ** attempt), 2_000)\n\n    if last_rtt_ms > 1_500:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n=======\n    # Switch endpoint on critical network errors or high latency\n    if error_type in {\"bad_gateway\", \"timeout\", \"connection_reset\", \"temporary_disconnect\"} or last_rtt_ms > 3_000:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    # Switch endpoint if we hit repeated congestion\n    if attempt >= 1 and error_type in {\"server_busy\", \"rate_limit\"} and consecutive_failures >= 2:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 0}\n\n    # Fail fast if total elapsed time is too high or max attempts reached\n    if attempt >= 3 or elapsed_ms > 14_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type == \"rate_limit\":\n        wait_ms = min(600 * (2 ** attempt), 8_000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(400 * (2 ** attempt), 4_000)\n    else:\n        wait_ms = min(250 * (2 ** attempt), 2_500)\n\n    # Add jitter/extra wait if RTT is creeping up\n    if last_rtt_ms > 1_500:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n>>>>>>> REPLACE"]}}, "artifacts_json": null, "artifact_dir": null, "embedding": null}
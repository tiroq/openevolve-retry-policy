{"id": "edeac15d-a796-4829-a260-d9c97041d7e8", "code": "\"\"\"Improved retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n# Define constants for retryable errors and valid actions\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n# Define a function to normalize the action payload\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n# Define the EVOLVE-BLOCK-START\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open or not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the error type is not retryable\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the number of consecutive failures is greater than or equal to 5\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5000}\n\n    # Calculate the base wait time based on the attempt number and error type\n    if error_type == \"rate_limit\":\n        base_wait_ms = min(500 * (2 ** attempt), 8000)\n    elif error_type == \"server_busy\":\n        base_wait_ms = min(250 * (2 ** attempt), 4000)\n    else:\n        base_wait_ms = min(100 * (2 ** attempt), 2000)\n\n    # Apply a jitter to the backoff duration to prevent synchronization attacks\n    jitter_ms = int(math.sqrt(elapsed_ms) * 100)\n    base_wait_ms += jitter_ms\n\n    # If the last round trip time is greater than 2000, add 250 to the base wait time\n    if last_rtt_ms > 2000:\n        base_wait_ms += 250\n\n    # Introduce a new feature: adaptive retry count\n    avg_retry_count = 2.0\n    if attempt >= 5:\n        avg_retry_count = 2.5\n    elif consecutive_failures >= 3:\n        avg_retry_count = 2.8\n\n    # Introduce a new feature: dynamic wait time increase\n    wait_ms = min(base_wait_ms, 20000)\n    if error_type == \"rate_limit\":\n        wait_ms = min(wait_ms, 10000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(wait_ms, 5000)\n\n    # Return the retry action with the calculated wait time and adaptive retry count\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms, \"avg_retry_count\": avg_retry_count}\n\n# EVOLVE-BLOCK-END\n\n# Introduce a new feature: dynamic retry count based on error type\ndef get_dynamic_retry_count(error_type: str) -> float:\n    if error_type == \"rate_limit\":\n        return 2.5\n    elif error_type == \"server_busy\":\n        return 2.8\n    else:\n        return 2.0", "changes_description": null, "language": "python", "parent_id": "e6f39818-1512-4907-b957-60b8c253b4c7", "generation": 3, "timestamp": 1777040652.915775, "iteration_found": 41, "metrics": {"runs_successfully": 0.0, "success_rate": 0.0, "avg_latency_ms": 999999.0, "avg_retry_count": 999.0, "dangerous_non_idempotent_retries": 999.0, "useless_retries": 999.0, "good_fail_fast_decisions": 0.0, "good_endpoint_switches": 0.0, "combined_score": -1000000000.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"runs_successfully": 1.0, "success_rate": 0.34375, "avg_latency_ms": 1044.375, "avg_retry_count": 2.125, "dangerous_non_idempotent_retries": 0.0, "useless_retries": 0.6875, "good_fail_fast_decisions": 0.28125, "good_endpoint_switches": 0.0, "combined_score": -6.89375}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: -6.8937\n- Feature coordinates: combined_score=-6.89, avg_retry_count=2.12\n- Focus areas: - Fitness unchanged at -6.8937\n- Exploring combined_score=-6.89, avg_retry_count=2.12 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: runs_successfully: 1.0000, success_rate: 0.3438, avg_latency_ms: 1044.3750, avg_retry_count: 2.1250, dangerous_non_idempotent_retries: 0.0000, useless_retries: 0.6875, good_fail_fast_decisions: 0.2812, good_endpoint_switches: 0.0000, combined_score: -6.8937\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: runs_successfully: 1.0000, success_rate: 0.3438, avg_latency_ms: 1044.3750, avg_retry_count: 2.1250, dangerous_non_idempotent_retries: 0.0000, useless_retries: 0.6875, good_fail_fast_decisions: 0.2812, good_endpoint_switches: 0.0000, combined_score: -6.8937\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: runs_successfully: 1.0000, success_rate: 0.3438, avg_latency_ms: 1044.3750, avg_retry_count: 2.1250, dangerous_non_idempotent_retries: 0.0000, useless_retries: 0.6875, good_fail_fast_decisions: 0.2812, good_endpoint_switches: 0.0000, combined_score: -6.8937\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: -6.8937)\n```python\n\"\"\"Improved retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n# Define constants for retryable errors and valid actions\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n# Define a function to normalize the action payload\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n# Define the EVOLVE-BLOCK-START\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the error type is not retryable\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the operation is not idempotent and the attempt is greater than 0\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the number of consecutive failures is greater than or equal to 5\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5000}\n\n    # Calculate the base wait time based on the attempt number\n    base_wait_ms = 100 * (2 ** attempt)\n    \n    # If the attempt is greater than or equal to 3, return fail action\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # If the error type is rate limit, set the base wait time to 8000\n    if error_type == \"rate_limit\":\n        base_wait_ms = min(base_wait_ms, 8000)\n    # If the error type is server busy, set the base wait time to 4000\n    elif error_type == \"server_busy\":\n        base_wait_ms = min(base_wait_ms, 4000)\n\n    # If the last round trip time is greater than 2000, add 250 to the base wait time\n    if last_rtt_ms > 2000:\n        base_wait_ms += 250\n\n    # Introduce a new feature: adaptive retry count\n    if attempt >= 5:\n        avg_retry_count = 2.5\n    elif consecutive_failures >= 3:\n        avg_retry_count = 2.8\n    else:\n        avg_retry_count = 2.0\n\n    # Return the retry action with the calculated wait time and adaptive retry count\n    return {\"action\": \"retry\", \"wait_ms\": min(base_wait_ms, 20000), \"avg_retry_count\": avg_retry_count}\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.3438), Performs well on avg_latency_ms (1044.3750), Performs well on avg_retry_count (2.1250), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (0.6875), Performs well on good_fail_fast_decisions (0.2812), Performs well on good_endpoint_switches (0.0000), Performs well on combined_score (-6.8937)\n\n### Program 2 (Score: -6.8937)\n```python\n\"\"\"Improved retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n# Define constants for retryable errors and valid actions\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n# Define a function to normalize the action payload\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n# Define the EVOLVE-BLOCK-START\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the error type is not retryable\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the operation is not idempotent and the attempt is greater than 0\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the number of consecutive failures is greater than or equal to 5\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5000}\n\n    # Calculate the base wait time based on the attempt number\n    base_wait_ms = 100 * (2 ** attempt)\n    \n    # If the attempt is greater than or equal to 3, return fail action\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # If the error type is rate limit, set the base wait time to 8000\n    if error_type == \"rate_limit\":\n        base_wait_ms = min(base_wait_ms, 8000)\n    # If the error type is server busy, set the base wait time to 4000\n    elif error_type == \"server_busy\":\n        base_wait_ms = min(base_wait_ms, 4000)\n\n    # If the last round trip time is greater than 2000, add 250 to the base wait time\n    if last_rtt_ms > 2000:\n        base_wait_ms += 250\n\n    # Return the retry action with the calculated wait time\n    return {\"action\": \"retry\", \"wait_ms\": min(base_wait_ms, 20000)}\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.3438), Performs well on avg_latency_ms (1044.3750), Performs well on avg_retry_count (2.1250), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (0.6875), Performs well on good_fail_fast_decisions (0.2812), Performs well on good_endpoint_switches (0.0000), Performs well on combined_score (-6.8937)\n\n### Program 3 (Score: -6.8937)\n```python\n\"\"\"Improved retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n# Define constants for retryable errors and valid actions\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n# Define a function to normalize the action payload\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n# Define the EVOLVE-BLOCK-START\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the error type is not retryable\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the operation is not idempotent and the attempt is greater than 0\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the number of consecutive failures is greater than or equal to 5\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5000}\n\n    # Calculate the base wait time based on the attempt number\n    base_wait_ms = 100 * (2 ** attempt)\n    \n    # If the attempt is greater than or equal to 3, return fail action\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # If the error type is rate limit, set the base wait time to 8000\n    if error_type == \"rate_limit\":\n        base_wait_ms = min(base_wait_ms, 8000)\n    # If the error type is server busy, set the base wait time to 4000\n    elif error_type == \"server_busy\":\n        base_wait_ms = min(base_wait_ms, 4000)\n\n    # If the last round trip time is greater than 2000, add 250 to the base wait time\n    if last_rtt_ms > 2000:\n        base_wait_ms += 250\n\n    # Return the retry action with the calculated wait time\n    return {\"action\": \"retry\", \"wait_ms\": min(base_wait_ms, 20000)}\n\n# Introduce a new feature: adaptive retry count\ndef get_adaptive_retry_count(attempt: int, consecutive_failures: int) -> float:\n    if attempt >= 5:\n        return 2.5\n    elif consecutive_failures >= 3:\n        return 2.8\n    else:\n        return 2.0\n\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.3438), Performs well on avg_latency_ms (1044.3750), Performs well on avg_retry_count (2.1250), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (0.6875), Performs well on good_fail_fast_decisions (0.2812), Performs well on good_endpoint_switches (0.0000), Performs well on combined_score (-6.8937)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: -6.8937)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5000}\n\n    base_wait_ms = 100 * (2 ** attempt)\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type == \"rate_limit\":\n        base_wait_ms = min(base_wait_ms, 8000)\n    elif error_type == \"server_busy\":\n        base_wait_ms = min(base_wait_ms, 4000)\n\n    if last_rtt_ms > 2000:\n        base_wait_ms += 250\n\n    return {\"action\": \"retry\", \"wait_ms\": min(base_wait_ms, 20000)}\n# EVOLVE-BLOCK-END\n```\nKey features: Alternative approach to runs_successfully, Alternative approach to success_rate\n\n### Program D2 (Score: -6.8937)\n```python\n\"\"\"Improved retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n# Define constants for retryable errors and valid actions\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n# Define a function to normalize the action payload\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n# Define the EVOLVE-BLOCK-START\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open or not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the error type is not retryable\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the number of consecutive failures is greater than or equal to 5\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5000}\n\n    # Calculate the base wait time based on the attempt number\n    base_wait_ms = 100 * (2 ** attempt)\n    \n    # If the attempt is greater than or equal to 3, return fail action\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # If the error type is rate limit, set the base wait time to 8000\n    if error_type == \"rate_limit\":\n        base_wait_ms = min(base_wait_ms, 8000)\n    # If the error type is server busy, set the base wait time to 4000\n    elif error_type == \"server_busy\":\n        base_wait_ms = min(base_wait_ms, 4000)\n\n    # If the last round trip time is greater than 2000, add 250 to the base wait time\n    if last_rtt_ms > 2000:\n        base_wait_ms += 250\n\n    # Introduce a new feature: adaptive retry count\n    if attempt >= 5:\n        avg_retry_count = 2.5\n    elif consecutive_failures >= 3:\n        avg_retry_count = 2.8\n    else:\n        avg_retry_count = 2.0\n\n    # Return the retry action with the calculated wait time and adaptive retry count\n    return {\"action\": \"retry\", \"wait_ms\": min(base_wait_ms, 20000), \"avg_retry_count\": avg_retry_count}\n# EVOLVE-BLOCK-END\n```\nKey features: Alternative approach to runs_successfully, Alternative approach to success_rate\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: -1000000000.0000, Type: Exploratory)\n```python\nBased on the provided code and the task, I will suggest a rewritten program that improves the FITNESS SCORE while maintaining diversity across the dimensions of `combined_score` and `avg_retry_count`.\n\nThe original program has a FITNESS SCORE of -6.8937 and focuses on the regions of solution space around `combined_score=-6.89` and `avg_retry_count=2.12`. The program uses a retry policy that adapts to different error types, attempts, and round trip times.\n\nTo improve the FITNESS SCORE, we can introduce a new feature that reduces the number of useless retries and increases the number of good fail-fast decisions. We can also explore different approaches to calculating the base wait time and the adaptive retry count.\n\nHere is the rewritten program:\n\n```python\n\"\"\"Improved retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n# Define constants for retryable errors and valid actions\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n# Define a function to normalize the action payload\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n# Define the EVOLVE-BLOCK-START\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open or not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the error type is not retryable\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the number of consecutive failures is greater than or equal to 5\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5000}\n\n    # Calculate the base wait time based on the attempt number\n    base_wait_ms = 100 * (2 ** attempt)\n    \n    # If the attempt is greater than or equal to 3, return fail action\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # If the error type is rate limit, set the base wait time to 8000\n    if error_type == \"rate_limit\":\n        base_wait_ms = min(base_wait_ms, 8000)\n    # If the error type is server busy, set the base wait time to 4000\n    elif error_type == \"server_busy\":\n        base_wait_ms = min(base_wait_ms, 4000)\n\n    # If the last round trip time is greater than 2000, add 250 to the base wait time\n    if last_rtt_ms > 2000:\n        base_wait_ms += 250\n\n    # Introduce a new feature: adaptive retry count\n    if attempt >= 5:\n        avg_retry_count = 2.5\n    elif consecutive_failures >= 3:\n        avg_retry_count = 2.8\n    else:\n        avg_retry_count = 2.0\n\n    # Return the retry action with the calculated wait time and adaptive retry count\n    return {\"action\": \"retry\", \"wait_ms\": min(base_wait_ms, 20000), \"avg_retry_count\": avg_retry_count}\n\n# Introduce a new feature: early fail-fast decision\ndef early_fail_fast_decision(attempt: int, error_type: str, last_rtt_ms: int) -> bool:\n    if attempt >= 3 and error_type in [\"rate_limit\", \"server_busy\"] and last_rtt_ms > 2000:\n\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: -176.7331, Type: Exploratory)\n```python\n\"\"\"Improved retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\nimport math\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n# Define the EVOLVE-BLOCK-START\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the error type is not retryable\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the operation is not idempotent and the attempt is greater than 0\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the number of consecutive failures is greater than or equal to 5\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5000}\n\n    # Calculate the base wait time based on the attempt number and error type\n    if error_type == \"rate_limit\":\n        base_wait_ms = min(500 * (2 ** attempt), 8000)\n    elif error_type == \"server_busy\":\n        base_wait_ms = min(250 * (2 ** attempt), 4000)\n    else:\n        base_wait_ms = min(100 * (2 ** attempt), 2000)\n\n    # Apply a jitter to the backoff duration to prevent synchronization attacks\n    jitter_ms = int(math.sqrt(elapsed_ms) * 100)\n    base_wait_ms += jitter_ms\n\n    # If the last round trip time is greater than 2000, add 250 to the base wait time\n    if last_rtt_ms > 2000:\n        base_wait_ms += 250\n\n    # Introduce a new feature: adaptive retry count\n    avg_retry_count = 2.0\n    if attempt >= 5:\n        avg_retry_count = 2.5\n    elif consecutive_failures >= 3:\n        avg_retry_count = 2.8\n\n    # Return the retry action with the calculated wait time and adaptive retry count\n    return {\"action\": \"retry\", \"wait_ms\": min(base_wait_ms, 20000), \"avg_retry_count\": avg_retry_count}\n\n# EVOLVE-BLOCK-END\n\n# Introduce a new feature: dynamic wait time increase\ndef get_dynamic_wait_time(attempt: int, error_type: str, last_rtt_ms: int) -> int:\n    if error_type == \"rate_limit\":\n        return min(100 * (2 ** attempt), 10000)\n    elif error_type == \"server_busy\":\n        return min(50 * (2 ** attempt), 5000)\n    else:\n        return min(100 * (2 ** attempt), 2000)\n\n# Introduce a new feature: dynamic retry count\ndef get_dynamic_retry_count(attempt: int, consecutive_failures: int) -> float:\n    if attempt >= 5:\n        return 2.5\n    elif consecutive_failures >= 3:\n        return 2.8\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: -1000000000.0000, Type: Exploratory)\n```python\nHere's a revised version of the program that improves its FITNESS SCORE by introducing new features to increase the wait time and retry count dynamically, while maintaining diversity across the dimensions of combined_score and avg_retry_count:\n\n```python\n\"\"\"Improved retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n# Define constants for retryable errors and valid actions\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n# Define a function to normalize the action payload\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n# Define the EVOLVE-BLOCK-START\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the error type is not retryable\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the operation is not idempotent and the attempt is greater than 0\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the number of consecutive failures is greater than or equal to 5\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5000}\n\n    # Calculate the base wait time based on the attempt number\n    base_wait_ms = 100 * (2 ** attempt)\n    \n    # If the attempt is greater than or equal to 3, return fail action\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # If the error type is rate limit, set the base wait time to 8000\n    if error_type == \"rate_limit\":\n        base_wait_ms = min(base_wait_ms, 8000)\n    # If the error type is server busy, set the base wait time to 4000\n    elif error_type == \"server_busy\":\n        base_wait_ms = min(base_wait_ms, 4000)\n\n    # If the last round trip time is greater than 2000, add 250 to the base wait time\n    if last_rtt_ms > 2000:\n        base_wait_ms += 250\n\n    # Introduce a new feature: adaptive retry count\n    if attempt >= 5:\n        avg_retry_count = 2.5\n    elif consecutive_failures >= 3:\n        avg_retry_count = 2.8\n    else:\n        avg_retry_count = 2.0\n\n    # Introduce a new feature: dynamic wait time increase\n    wait_ms = min(base_wait_ms, 20000)\n    if error_type == \"rate_limit\":\n        wait_ms = min(wait_ms, 10000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(wait_ms, 5000)\n\n    # Return the retry action with the calculated wait time and adaptive retry count\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms, \"avg_retry_count\": avg_retry_count}\n\n# EVOLVE-BLOCK-END\n\n# Introduce a new feature: dynamic retry count based on error type\ndef get_dynamic_retry_count(error_type: str) -> float:\n    if error_type == \"rate_limit\":\n        return 2.5\n    elif error_type == \"server_busy\":\n        return 2.8\n    else:\n        return 2.0\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n\"\"\"Improved retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n# Define constants for retryable errors and valid actions\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n# Define a function to normalize the action payload\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n# Define the EVOLVE-BLOCK-START\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the error type is not retryable\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the operation is not idempotent and the attempt is greater than 0\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the number of consecutive failures is greater than or equal to 5\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5000}\n\n    # Calculate the base wait time based on the attempt number\n    base_wait_ms = 100 * (2 ** attempt)\n    \n    # If the attempt is greater than or equal to 3, return fail action\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # If the error type is rate limit, set the base wait time to 8000\n    if error_type == \"rate_limit\":\n        base_wait_ms = min(base_wait_ms, 8000)\n    # If the error type is server busy, set the base wait time to 4000\n    elif error_type == \"server_busy\":\n        base_wait_ms = min(base_wait_ms, 4000)\n\n    # If the last round trip time is greater than 2000, add 250 to the base wait time\n    if last_rtt_ms > 2000:\n        base_wait_ms += 250\n\n    # Return the retry action with the calculated wait time\n    return {\"action\": \"retry\", \"wait_ms\": min(base_wait_ms, 20000)}\n# EVOLVE-BLOCK-END\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: combined_score, avg_retry_count\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["Here is the rewritten program that improves its FITNESS SCORE while maintaining diversity across the dimensions of `combined_score` and `avg_retry_count`:\n\n```python\n\"\"\"Improved retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n# Define constants for retryable errors and valid actions\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n# Define a function to normalize the action payload\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n# Define the EVOLVE-BLOCK-START\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open or not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the error type is not retryable\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Check if the number of consecutive failures is greater than or equal to 5\n    if consecutive_failures >= 5:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5000}\n\n    # Calculate the base wait time based on the attempt number and error type\n    if error_type == \"rate_limit\":\n        base_wait_ms = min(500 * (2 ** attempt), 8000)\n    elif error_type == \"server_busy\":\n        base_wait_ms = min(250 * (2 ** attempt), 4000)\n    else:\n        base_wait_ms = min(100 * (2 ** attempt), 2000)\n\n    # Apply a jitter to the backoff duration to prevent synchronization attacks\n    jitter_ms = int(math.sqrt(elapsed_ms) * 100)\n    base_wait_ms += jitter_ms\n\n    # If the last round trip time is greater than 2000, add 250 to the base wait time\n    if last_rtt_ms > 2000:\n        base_wait_ms += 250\n\n    # Introduce a new feature: adaptive retry count\n    avg_retry_count = 2.0\n    if attempt >= 5:\n        avg_retry_count = 2.5\n    elif consecutive_failures >= 3:\n        avg_retry_count = 2.8\n\n    # Introduce a new feature: dynamic wait time increase\n    wait_ms = min(base_wait_ms, 20000)\n    if error_type == \"rate_limit\":\n        wait_ms = min(wait_ms, 10000)\n    elif error_type == \"server_busy\":\n        wait_ms = min(wait_ms, 5000)\n\n    # Return the retry action with the calculated wait time and adaptive retry count\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms, \"avg_retry_count\": avg_retry_count}\n\n# EVOLVE-BLOCK-END\n\n# Introduce a new feature: dynamic retry count based on error type\ndef get_dynamic_retry_count(error_type: str) -> float:\n    if error_type == \"rate_limit\":\n        return 2.5\n    elif error_type == \"server_busy\":\n        return 2.8\n    else:\n        return 2.0\n```\n\nThis rewritten program introduces new features to increase the wait time and retry count dynamically, while maintaining diversity across the dimensions of `combined_score` and `avg_retry_count`. The `choose_action` function calculates the base wait time based on the attempt number and error type, and applies a jitter to the backoff duration to prevent synchronization attacks. The program also introduces a new feature"]}}, "artifacts_json": null, "artifact_dir": null, "embedding": null}
{"id": "6ebdf116-82b3-46e5-8f23-779f02d5bbe0", "code": "```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"\n    Optimized retry policy to maximize fitness score.\n    \n    Key Strategy:\n    1. Strict Fail-Fast: Cap at 2 attempts (0, 1) to drastically reduce 'useless_retries' \n       and 'avg_latency_ms', which are key drivers of the combined_score.\n    2. Aggressive Endpoint Switching: Use 'switch_endpoint' on the first retry (attempt 1)\n       for infrastructure-level errors to bypass bad nodes immediately.\n    3. Idempotency Guard: Absolute zero tolerance for retrying non-idempotent requests.\n    4. Rapid Circuit Breaking: Trigger on 3 consecutive failures to avoid cascading.\n    5. Tiered Backoff: Differentiated wait times based on the nature of the error.\n    \"\"\"\n    # Immediate fail if circuit is open or error is not retryable\n    if circuit_open or error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Non-idempotent requests must never be retried\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: Trigger rapidly on failure streaks\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # Fail fast: Limit to 2 total attempts (0 and 1).\n    # This is the most critical optimization for the 'useless_retries' metric.\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on the first retry (attempt 1) for network/gateway-level failures.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered Exponential Backoff\n    if error_type == \"rate_limit\":\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance: Increase delay if network latency (RTT) is high\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_", "changes_description": null, "language": "python", "parent_id": "bc523a81-6c59-4861-892b-546e29cddccd", "generation": 5, "timestamp": 1776873735.6342201, "iteration_found": 40, "metrics": {"runs_successfully": 0.0, "success_rate": 0.0, "avg_latency_ms": 999999.0, "avg_retry_count": 999.0, "dangerous_non_idempotent_retries": 999.0, "useless_retries": 999.0, "good_fail_fast_decisions": 0.0, "good_endpoint_switches": 0.0, "combined_score": -1000000000.0}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"runs_successfully": 1.0, "success_rate": 0.46875, "avg_latency_ms": 1494.6875, "avg_retry_count": 1.84375, "dangerous_non_idempotent_retries": 0.0, "useless_retries": 1.0, "good_fail_fast_decisions": 0.28125, "good_endpoint_switches": 0.09375, "combined_score": -9.85625}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: -9.8562\n- Feature coordinates: combined_score=-9.86, avg_retry_count=1.84\n- Focus areas: - Fitness declined: 6.1844 \u2192 -9.8562. Consider revising recent changes.\n- Exploring combined_score=-9.86, avg_retry_count=1.84 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4688, avg_latency_ms: 1136.5625, avg_retry_count: 1.5000, dangerous_non_idempotent_retries: 0.0000, useless_retries: 0.6562, good_fail_fast_decisions: 0.4062, good_endpoint_switches: 0.0938, combined_score: 6.1844\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4688, avg_latency_ms: 1136.5625, avg_retry_count: 1.5000, dangerous_non_idempotent_retries: 0.0000, useless_retries: 0.6562, good_fail_fast_decisions: 0.4062, good_endpoint_switches: 0.0938, combined_score: 6.1844\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4688, avg_latency_ms: 1136.5625, avg_retry_count: 1.5000, dangerous_non_idempotent_retries: 0.0000, useless_retries: 0.6562, good_fail_fast_decisions: 0.4062, good_endpoint_switches: 0.0938, combined_score: 6.1844\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 6.1844)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"\n    Optimized retry policy to maximize combined_score.\n    \n    Improvements:\n    1. Aggressive Fail-Fast: Strictly limit to 2 attempts (0, 1) to minimize useless_retries.\n    2. High-Precision Switching: Target infra errors immediately on attempt 1.\n    3. Idempotency Guard: zero tolerance for non-idempotent retries.\n    4. Dynamic Backoff: Adjusted wait times for specific error types.\n    \"\"\"\n    # Immediate exit for circuit breakers or non-retryable errors\n    if circuit_open or error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Never retry non-idempotent requests (only attempt 0 is allowed)\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: Trigger rapidly on failure streaks\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # Fail fast: This is the critical driver for reducing 'useless_retries' and latency\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on the first retry for network-level failures to bypass bad nodes.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered Backoff\n    if error_type == \"rate_limit\":\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance for high RTT\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4688), Performs well on avg_latency_ms (1136.5625), Performs well on avg_retry_count (1.5000), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (0.6562), Performs well on good_fail_fast_decisions (0.4062), Performs well on good_endpoint_switches (0.0938), Performs well on combined_score (6.1844)\n\n### Program 2 (Score: 6.1844)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"\n    Optimized retry policy focused on maximizing combined_score.\n    \n    Strategy:\n    1. Strict Fail-Fast: Limit total attempts to 2 (attempt 0 and 1) to minimize useless_retries.\n    2. High-Precision Switching: Target infra-errors on the first retry to bypass faulty nodes.\n    3. Idempotency Safety: Zero tolerance for non-idempotent retries.\n    4. Circuit Breaking: Rapidly isolate failing systems.\n    \"\"\"\n    # Immediate fail conditions\n    if circuit_open or error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Never retry non-idempotent requests\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: Trigger on 3 consecutive failures\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # Fail fast: Cap at 2 attempts to optimize latency and useless_retries metric\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on the first retry (attempt 1) for network/gateway errors.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered Backoff\n    if error_type == \"rate_limit\":\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance for high RTT\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4688), Performs well on avg_latency_ms (1136.5625), Performs well on avg_retry_count (1.5000), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (0.6562), Performs well on good_fail_fast_decisions (0.4062), Performs well on good_endpoint_switches (0.0938), Performs well on combined_score (6.1844)\n\n### Program 3 (Score: 6.1844)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"\n    Optimized retry policy to maximize combined_score.\n    \n    Strategy:\n    1. High-Efficiency Fail-Fast: Strict limit of 2 attempts total to minimize useless_retries.\n    2. Precise Endpoint Switching: Target network/gateway errors immediately on attempt 1.\n    3. Idempotency Safety: Absolute zero tolerance for non-idempotent retries.\n    4. Circuit Breaking: Rapidly isolate systems on failure streaks.\n    5. Dynamic Backoff: Tiered wait times adjusted for RTT and error severity.\n    \"\"\"\n    # Immediate fail if circuit is open or error is not retryable\n    if circuit_open or error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Non-idempotent requests must never be retried (only attempt 0 is allowed)\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: Open circuit quickly (3 failures) to prevent cascading\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # Fail fast: Limit to 2 attempts (0 and 1). \n    # This is the primary driver for reducing 'useless_retries' and 'avg_latency_ms'.\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on the first retry (attempt 1) for infrastructure-level failures.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered Exponential Backoff\n    if error_type == \"rate_limit\":\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance: Add delay if the network shows high latency (RTT)\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4688), Performs well on avg_latency_ms (1136.5625), Performs well on avg_retry_count (1.5000), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (0.6562), Performs well on good_fail_fast_decisions (0.4062), Performs well on good_endpoint_switches (0.0938), Performs well on combined_score (6.1844)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 6.1844)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"\n    Optimized retry policy to maximize combined_score.\n    \n    Strategy:\n    1. Strict Fail-Fast: Limit total attempts to 2 (attempt 0 and 1) to minimize useless_retries.\n    2. High-Precision Switching: Target infra-errors on the first retry to bypass faulty nodes.\n    3. Idempotency Safety: Zero tolerance for non-idempotent retries.\n    4. Circuit Breaking: Rapidly isolate failing systems.\n    \"\"\"\n    # Immediate fail conditions\n    if circuit_open or error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Never retry non-idempotent requests\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: Trigger on 3 consecutive failures\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # Fail fast: Cap at 2 attempts to optimize latency and useless_retries metric\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on the first retry (attempt 1) for network/gateway errors.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered Backoff\n    if error_type == \"rate_limit\":\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance for high RTT\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nKey features: Alternative approach to runs_successfully, Alternative approach to success_rate\n\n### Program D2 (Score: 6.1844)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Reverting to the high-performing strategy from Program 1/2/3.\n    The core of the high fitness score was a strict attempt limit (attempt < 2)\n    and precise endpoint switching for network-level errors.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Never retry non-idempotent requests beyond the first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: Trigger on 3 consecutive failures to prevent cascading\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # Fail fast: Limit to 2 attempts total (attempt 0, 1). \n    # This is the critical change to reduce 'useless_retries' and lower latency.\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on the first retry (attempt 1) for infrastructure-level errors.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered Exponential Backoff\n    if error_type == \"rate_limit\":\n        # Rate limits need substantial time to reset\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is transient but needs a small gap\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        # Fast recovery for transient network glitches\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance: Add delay if the network is showing high latency\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nKey features: Alternative approach to runs_successfully, Alternative approach to success_rate\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: -8.8906, Type: Exploratory)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Optimized to improve success rate and reduce useless retries by introducing \n    endpoint switching and smarter fail-fast logic.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strict idempotency check to avoid dangerous retries\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker logic based on failure streaks\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Fail fast if we've exhausted reasonable attempts\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategy: Switch endpoints on specific network-level errors to bypass localized issues\n    if error_type in {\"bad_gateway\", \"connection_reset\", \"timeout\"} and attempt >= 1:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Dynamic backoff based on error type\n    if error_type == \"rate_limit\":\n        wait_ms = 1000 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        wait_ms = 500 * (2 ** attempt)\n    else:\n        wait_ms = 200 * (2 ** attempt)\n\n    # Adjust for high latency\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: 6.1844, Type: High-Performer)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Reverting to the high-performing logic from Top Programs.\n    Key optimizations:\n    1. Strict fail-fast at attempt 2 to eliminate 'useless_retries'.\n    2. Aggressive endpoint switching on the first retry for infra errors.\n    3. Tiered exponential backoff to balance latency and success rate.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Never retry non-idempotent requests beyond the first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: Trigger on a moderate streak to prevent cascading failure\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # Fail fast: Limit to 2 attempts total (attempt 0, 1). \n    # This is the primary driver for reducing 'useless_retries' and latency.\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on the first retry (attempt 1) for infrastructure-level errors.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered Exponential Backoff\n    if error_type == \"rate_limit\":\n        # Rate limits need substantial time to reset\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is transient but needs a small gap\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        # Fast recovery for transient network glitches\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance: Add delay if the network is showing high latency\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: -7.0563, Type: Exploratory)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    This version integrates the high-performing logic from Program 1,\n    focusing on strategic endpoint switching and differentiated backoff\n    to maximize success rate while maintaining low latency.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: No retries for non-idempotent requests after the first failure\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker for high failure streaks\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Fail fast to avoid excessive latency and useless retries\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Target network-level errors that are likely solved by switching endpoints.\n    # Trigger on attempt 1 (second try) to verify the failure wasn't a fluke.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if error_type in switch_triggers and attempt >= 1:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 150}\n\n    # Differentiated Exponential Backoff\n    if error_type == \"rate_limit\":\n        # Rate limits require longer, stable pauses to recover\n        wait_ms = 1200 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is usually transient\n        wait_ms = 600 * (2 ** attempt)\n    else:\n        # General network errors use a tighter backoff for lower latency\n        wait_ms = 200 * (2 ** attempt)\n\n    # Dynamic latency adjustment:\n    # If RTT is high, increase wait to prevent congestion collapse\n    if last_rtt_ms > 1200:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Optimized to maximize success rate while reducing useless retries \n    and latency. This version tightens the fail-fast logic and \n    refines endpoint switching to be more aggressive on the first retry.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Non-idempotent requests must not be retried after initial failure\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker for systemic failures\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Fail fast to prevent latency bloat and useless retries\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on the first retry (attempt 1) for infrastructure-related errors.\n    # This bypasses problematic nodes early.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Differentiated Backoff Strategy\n    if error_type == \"rate_limit\":\n        # Rate limits need substantial cooling-off periods\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is transient; moderate wait\n        wait_ms = 700 * (2 ** attempt)\n    else:\n        # General network errors use a lean backoff to minimize latency\n        wait_ms = 250 * (2 ** attempt)\n\n    # Congestion Control: If RTT is high, increase wait to avoid exacerbating the issue\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 8_000)}\n# EVOLVE-BLOCK-END\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: combined_score, avg_retry_count\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"\n    Optimized retry policy to maximize fitness score.\n    \n    Key Strategy:\n    1. Strict Fail-Fast: Cap at 2 attempts (0, 1) to drastically reduce 'useless_retries' \n       and 'avg_latency_ms', which are key drivers of the combined_score.\n    2. Aggressive Endpoint Switching: Use 'switch_endpoint' on the first retry (attempt 1)\n       for infrastructure-level errors to bypass bad nodes immediately.\n    3. Idempotency Guard: Absolute zero tolerance for retrying non-idempotent requests.\n    4. Rapid Circuit Breaking: Trigger on 3 consecutive failures to avoid cascading.\n    5. Tiered Backoff: Differentiated wait times based on the nature of the error.\n    \"\"\"\n    # Immediate fail if circuit is open or error is not retryable\n    if circuit_open or error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Non-idempotent requests must never be retried\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: Trigger rapidly on failure streaks\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # Fail fast: Limit to 2 total attempts (0 and 1).\n    # This is the most critical optimization for the 'useless_retries' metric.\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on the first retry (attempt 1) for network/gateway-level failures.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered Exponential Backoff\n    if error_type == \"rate_limit\":\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance: Increase delay if network latency (RTT) is high\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_"]}}, "artifacts_json": null, "artifact_dir": null, "embedding": null}
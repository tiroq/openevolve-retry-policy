{"id": "6ab7f82b-49eb-4e49-b702-776eeb3a9c3d", "code": "\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Reverting to the high-performing baseline (Program 1/2/3).\n    The critical factors for the high score were:\n    1. Strict attempt limit (attempt < 2) to minimize 'useless_retries' and latency.\n    2. Immediate endpoint switching on the first retry for infrastructure errors.\n    3. Tiered backoff specifically tuned for different error types.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Never retry non-idempotent requests beyond the first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: Trigger on 3 consecutive failures to prevent cascading\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # Fail fast: Limit to 2 attempts total (attempt 0, 1). \n    # This is the most critical change to reduce useless retries and latency.\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on the first retry (attempt 1) for infrastructure-level errors.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered Exponential Backoff\n    if error_type == \"rate_limit\":\n        # Rate limits need substantial time to reset\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is transient but needs a small gap\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        # Fast recovery for transient network glitches\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance: Add delay if the network is showing high latency\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END", "changes_description": null, "language": "python", "parent_id": "b487cc41-5f08-49ac-bc99-56341c189fe0", "generation": 3, "timestamp": 1776873209.415445, "iteration_found": 27, "metrics": {"runs_successfully": 1.0, "success_rate": 0.46875, "avg_latency_ms": 1136.5625, "avg_retry_count": 1.5, "dangerous_non_idempotent_retries": 0.0, "useless_retries": 0.65625, "good_fail_fast_decisions": 0.40625, "good_endpoint_switches": 0.09375, "combined_score": 6.184375}, "complexity": 0.0, "diversity": 0.0, "metadata": {"changes": "Full rewrite", "parent_metrics": {"runs_successfully": 1.0, "success_rate": 0.46875, "avg_latency_ms": 1398.75, "avg_retry_count": 1.875, "dangerous_non_idempotent_retries": 0.0, "useless_retries": 1.0, "good_fail_fast_decisions": 0.28125, "good_endpoint_switches": 0.09375, "combined_score": -7.05625}, "island": 0}, "prompts": {"full_rewrite_user": {"system": "You are an expert software developer tasked with iteratively improving a codebase.\nYour goal is to maximize the FITNESS SCORE while exploring diverse solutions across feature dimensions.\nThe system maintains a collection of diverse programs - both high fitness AND diversity are valuable.", "user": "# Current Program Information\n- Fitness: -7.0563\n- Feature coordinates: combined_score=-7.06, avg_retry_count=1.88\n- Focus areas: - Fitness declined: 6.1844 \u2192 -7.0563. Consider revising recent changes.\n- Exploring combined_score=-7.06, avg_retry_count=1.88 region of solution space\n- Consider simplifying - code length exceeds 500 characters\n\n\n\n# Program Evolution History\n## Previous Attempts\n\n### Attempt 3\n- Changes: Full rewrite\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4688, avg_latency_ms: 1136.5625, avg_retry_count: 1.5000, dangerous_non_idempotent_retries: 0.0000, useless_retries: 0.6562, good_fail_fast_decisions: 0.4062, good_endpoint_switches: 0.0938, combined_score: 6.1844\n- Outcome: Mixed results\n\n### Attempt 2\n- Changes: Full rewrite\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4688, avg_latency_ms: 1136.5625, avg_retry_count: 1.5000, dangerous_non_idempotent_retries: 0.0000, useless_retries: 0.6562, good_fail_fast_decisions: 0.4062, good_endpoint_switches: 0.0938, combined_score: 6.1844\n- Outcome: Mixed results\n\n### Attempt 1\n- Changes: Full rewrite\n- Metrics: runs_successfully: 1.0000, success_rate: 0.4688, avg_latency_ms: 1136.5625, avg_retry_count: 1.5000, dangerous_non_idempotent_retries: 0.0000, useless_retries: 0.6562, good_fail_fast_decisions: 0.4062, good_endpoint_switches: 0.0938, combined_score: 6.1844\n- Outcome: Mixed results\n\n## Top Performing Programs\n\n### Program 1 (Score: 6.1844)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Reverting to the high-performing strategy from Program 1/2/3.\n    The core of the high fitness score was a strict attempt limit (attempt < 2)\n    and precise endpoint switching for network-level errors.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Never retry non-idempotent requests beyond the first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: Trigger on 3 consecutive failures to prevent cascading\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # Fail fast: Limit to 2 attempts total (attempt 0, 1). \n    # This is the critical change to reduce 'useless_retries' and lower latency.\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on the first retry (attempt 1) for infrastructure-level errors.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered Exponential Backoff\n    if error_type == \"rate_limit\":\n        # Rate limits need substantial time to reset\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is transient but needs a small gap\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        # Fast recovery for transient network glitches\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance: Add delay if the network is showing high latency\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4688), Performs well on avg_latency_ms (1136.5625), Performs well on avg_retry_count (1.5000), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (0.6562), Performs well on good_fail_fast_decisions (0.4062), Performs well on good_endpoint_switches (0.0938), Performs well on combined_score (6.1844)\n\n### Program 2 (Score: 6.1844)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Refined to maximize fitness by aggressively pruning useless retries\n    and optimizing endpoint switching for network-level failures.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Never retry non-idempotent requests\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: Trigger on a moderate streak to prevent cascading failure\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # Fail fast: Limit to 2 attempts total to minimize 'useless_retries' and latency\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on the first retry (attempt 1) for infrastructure-level errors.\n    # This bypasses bad nodes quickly.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered Exponential Backoff\n    if error_type == \"rate_limit\":\n        # Rate limits need substantial time to reset\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is transient but needs a small gap\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        # Fast recovery for transient network glitches\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance: Add delay if the network is showing high latency\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4688), Performs well on avg_latency_ms (1136.5625), Performs well on avg_retry_count (1.5000), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (0.6562), Performs well on good_fail_fast_decisions (0.4062), Performs well on good_endpoint_switches (0.0938), Performs well on combined_score (6.1844)\n\n### Program 3 (Score: 6.1844)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Reverting to a high-performing configuration that balances success rate \n    and latency. This version focuses on a strict fail-fast limit (attempt < 2) \n    to eliminate 'useless_retries' while using aggressive endpoint switching \n    to recover from infrastructure failures.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Never retry non-idempotent requests beyond the first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: Trigger on a moderate streak to prevent cascading failure\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # Fail fast: Limit to 2 attempts total (attempt 0, 1). \n    # This significantly reduces 'useless_retries' and avg_latency.\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on the first retry (attempt 1) for infrastructure-level errors.\n    # This bypasses bad nodes quickly.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered Exponential Backoff\n    if error_type == \"rate_limit\":\n        # Rate limits need substantial time to reset\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is transient but needs a small gap\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        # Fast recovery for transient network glitches\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance: Add delay if the network is showing high latency\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nKey features: Performs well on runs_successfully (1.0000), Performs well on success_rate (0.4688), Performs well on avg_latency_ms (1136.5625), Performs well on avg_retry_count (1.5000), Performs well on dangerous_non_idempotent_retries (0.0000), Performs well on useless_retries (0.6562), Performs well on good_fail_fast_decisions (0.4062), Performs well on good_endpoint_switches (0.0938), Performs well on combined_score (6.1844)\n\n\n\n## Diverse Programs\n\n### Program D1 (Score: 6.1844)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Optimized to maximize fitness by minimizing useless_retries and \n    maximizing good_fail_fast_decisions. This version focuses on a \n    strict retry budget and high-precision endpoint switching.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Non-idempotent requests must not be retried after the first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Fail fast: Strict cap at 2 attempts (total 3 tries) to prune useless retries\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: Open circuit on a moderate failure streak\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # Strategic Endpoint Switching:\n    # Target network-level failures on the first retry to bypass faulty nodes.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"temporary_disconnect\", \"timeout\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered Exponential Backoff\n    if error_type == \"rate_limit\":\n        # Rate limits require substantial cooldown\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is transient\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        # Fast recovery for transient network glitches\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance: Add delay if network latency is high\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nKey features: Alternative approach to runs_successfully, Alternative approach to success_rate\n\n### Program D2 (Score: 6.1844)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Reverting to the high-performing logic from Program 1 & 2.\n    Key optimizations:\n    1. Strict fail-fast at attempt 2 to eliminate 'useless_retries'.\n    2. Aggressive endpoint switching on the first retry for infra errors.\n    3. Tiered exponential backoff to balance latency and success rate.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Never retry non-idempotent requests beyond the first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: Trigger on a moderate streak to prevent cascading failure\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # Fail fast: Limit to 2 attempts total (attempt 0, 1). \n    # This is the primary driver for reducing 'useless_retries' and latency.\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on the first retry (attempt 1) for infrastructure-level errors.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered Exponential Backoff\n    if error_type == \"rate_limit\":\n        # Rate limits need substantial time to reset\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is transient but needs a small gap\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        # Fast recovery for transient network glitches\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance: Add delay if the network is showing high latency\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nKey features: Alternative approach to runs_successfully, Alternative approach to success_rate\n\n## Inspiration Programs\n\nThese programs represent diverse approaches and creative solutions that may inspire new ideas:\n\n### Inspiration 1 (Score: -45.8844, Type: Exploratory)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Contract:\n    - action in {retry, fail, switch_endpoint, open_circuit}\n    - wait_ms is a non-negative integer\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Improve fail-fast and endpoint switching\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 2_000}\n\n    if attempt >= 3:\n        # Try switching endpoint as a last resort before failing\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 500}\n\n    # Strategy: Fail fast on high latency or specific errors\n    if last_rtt_ms > 5_000:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Differentiated backoff\n    if error_type == \"rate_limit\":\n        # Aggressive backoff for rate limits to avoid further penalties\n        wait_ms = 1000 * (3 ** attempt)\n    elif error_type in {\"server_busy\", \"bad_gateway\"}:\n        # Moderate backoff for server-side issues\n        wait_ms = 500 * (2 ** attempt)\n    else:\n        # Short backoff for transient network issues\n        wait_ms = 200 * (2 ** attempt)\n\n    # Cap the wait time\n    wait_ms = min(wait_ms, 10_000)\n\n    return {\"action\": \"retry\", \"wait_ms\": wait_ms}\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 2 (Score: -9.4187, Type: Exploratory)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Optimized to revert to a high-performing baseline while improving \n    endpoint switching and fail-fast logic.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: No retries for non-idempotent requests after the first failure\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: trigger on failure streaks to avoid hammering a dead system\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Fail fast after 3 attempts to keep avg_latency_ms low\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on network/gateway errors on the second attempt (attempt == 1)\n    # to bypass localized infrastructure issues without excessive switching.\n    if attempt >= 1 and error_type in {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\"}:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 150}\n\n    # Tiered Exponential Backoff\n    if error_type == \"rate_limit\":\n        # Rate limits require significant pauses\n        wait_ms = 1200 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load issues require moderate backoff\n        wait_ms = 600 * (2 ** attempt)\n    else:\n        # General transient network errors use tighter backoff\n        wait_ms = 250 * (2 ** attempt)\n\n    # Adjust for high RTT to prevent congestion\n    if last_rtt_ms > 1200:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n### Inspiration 3 (Score: 6.1844, Type: High-Performer)\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Reverting to the high-performing strategy from Program 1/2/3.\n    The core of the high fitness score was a strict attempt limit (attempt < 2)\n    and precise endpoint switching for network-level errors.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Never retry non-idempotent requests beyond the first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: Trigger on 3 consecutive failures to prevent cascading\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # Fail fast: Limit to 2 attempts total (attempt 0, 1). \n    # This is the critical change to reduce 'useless_retries' and lower latency.\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on the first retry (attempt 1) for infrastructure-level errors.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered Exponential Backoff\n    if error_type == \"rate_limit\":\n        # Rate limits need substantial time to reset\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is transient but needs a small gap\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        # Fast recovery for transient network glitches\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance: Add delay if the network is showing high latency\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\nUnique approach: Modification:, [Fragment formatting error: 'metric_name'], [Fragment formatting error: 'metric_name']\n\n# Current Program\n```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    This version reverts to the high-performing logic of the top programs,\n    expanding the set of endpoint-switching triggers and refining the \n    backoff intervals to maximize success rate while minimizing latency.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: No retries for non-idempotent requests after the first failure\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker for high failure streaks\n    if consecutive_failures >= 4:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 5_000}\n\n    # Fail fast to avoid excessive latency and useless retries\n    if attempt >= 3:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Target network-level errors that are likely solved by switching endpoints.\n    # We trigger on attempt 1 (second try) to verify the failure wasn't a fluke.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if error_type in switch_triggers and attempt >= 1:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 150}\n\n    # Differentiated Exponential Backoff\n    if error_type == \"rate_limit\":\n        # Rate limits require longer, stable pauses to recover\n        wait_ms = 1200 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is usually transient\n        wait_ms = 600 * (2 ** attempt)\n    else:\n        # General network errors use a tighter backoff for lower latency\n        wait_ms = 200 * (2 ** attempt)\n\n    # Dynamic latency adjustment:\n    # If RTT is high, increase wait to prevent congestion collapse\n    if last_rtt_ms > 1200:\n        wait_ms += 300\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```\n\n# Task\nRewrite the program to improve its FITNESS SCORE.\nThe system maintains diversity across these dimensions: combined_score, avg_retry_count\nDifferent solutions with similar fitness but different features are valuable.\nProvide the complete new program code.\n\nIMPORTANT: Make sure your rewritten program maintains the same inputs and outputs\nas the original program, but with improved internal implementation.\n\n```python\n# Your rewritten program here\n```", "responses": ["```python\n\"\"\"Initial retry policy for OpenEvolve.\n\nOpenEvolve should mutate only the EVOLVE-BLOCK below.\nEverything outside the block is treated as stable helper code.\n\"\"\"\n\nfrom __future__ import annotations\n\nfrom typing import Dict\n\n\nRETRYABLE_ERRORS = {\n    \"timeout\",\n    \"connection_reset\",\n    \"server_busy\",\n    \"rate_limit\",\n    \"bad_gateway\",\n    \"temporary_disconnect\",\n    \"slow_response\",\n}\n\nVALID_ACTIONS = {\"retry\", \"fail\", \"switch_endpoint\", \"open_circuit\"}\n\n\ndef normalize_action(payload: Dict[str, int | str]) -> Dict[str, int | str]:\n    \"\"\"Defensive normalization used by the evaluator.\n\n    The evolve block should still try to return correct values directly.\n    \"\"\"\n    action = payload.get(\"action\", \"fail\")\n    if action not in VALID_ACTIONS:\n        action = \"fail\"\n\n    wait_ms = payload.get(\"wait_ms\", 0)\n    if not isinstance(wait_ms, int):\n        try:\n            wait_ms = int(wait_ms)\n        except Exception:\n            wait_ms = 0\n    wait_ms = max(0, min(wait_ms, 30_000))\n    return {\"action\": action, \"wait_ms\": wait_ms}\n\n\n# EVOLVE-BLOCK-START\ndef choose_action(\n    attempt: int,\n    error_type: str,\n    elapsed_ms: int,\n    last_rtt_ms: int,\n    consecutive_failures: int,\n    is_idempotent: bool,\n    circuit_open: bool,\n) -> Dict[str, int | str]:\n    \"\"\"Return the next retry-control action.\n\n    Reverting to the high-performing baseline (Program 1/2/3).\n    The critical factors for the high score were:\n    1. Strict attempt limit (attempt < 2) to minimize 'useless_retries' and latency.\n    2. Immediate endpoint switching on the first retry for infrastructure errors.\n    3. Tiered backoff specifically tuned for different error types.\n    \"\"\"\n    if circuit_open:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    if error_type not in RETRYABLE_ERRORS:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Safety: Never retry non-idempotent requests beyond the first attempt\n    if not is_idempotent and attempt > 0:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Circuit breaker: Trigger on 3 consecutive failures to prevent cascading\n    if consecutive_failures >= 3:\n        return {\"action\": \"open_circuit\", \"wait_ms\": 3000}\n\n    # Fail fast: Limit to 2 attempts total (attempt 0, 1). \n    # This is the most critical change to reduce useless retries and latency.\n    if attempt >= 2:\n        return {\"action\": \"fail\", \"wait_ms\": 0}\n\n    # Strategic Endpoint Switching:\n    # Switch on the first retry (attempt 1) for infrastructure-level errors.\n    switch_triggers = {\"bad_gateway\", \"connection_reset\", \"timeout\", \"temporary_disconnect\", \"slow_response\"}\n    if attempt >= 1 and error_type in switch_triggers:\n        return {\"action\": \"switch_endpoint\", \"wait_ms\": 100}\n\n    # Tiered Exponential Backoff\n    if error_type == \"rate_limit\":\n        # Rate limits need substantial time to reset\n        wait_ms = 1500 * (2 ** attempt)\n    elif error_type == \"server_busy\":\n        # Server load is transient but needs a small gap\n        wait_ms = 800 * (2 ** attempt)\n    else:\n        # Fast recovery for transient network glitches\n        wait_ms = 300 * (2 ** attempt)\n\n    # Congestion avoidance: Add delay if the network is showing high latency\n    if last_rtt_ms > 1000:\n        wait_ms += 200\n\n    return {\"action\": \"retry\", \"wait_ms\": min(wait_ms, 10_000)}\n# EVOLVE-BLOCK-END\n```"]}}, "artifacts_json": null, "artifact_dir": null, "embedding": null}